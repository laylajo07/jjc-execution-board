1. 나는 테크핀레이팅스 회사의 PM이야
2. 사내 AI 해커톤 대회가 열려서 나도 참가할 예정이야.
3. AI 팀에서 사내 개발/비개발 인원들이 개발을 쉽게 할 수 있도록 웹 페이지를 제공하고 있어. 웹페이지를 들어가면 현재 내 상황이나 개발하고 싶은 내용을 입력하면, 프롬프트가 나오고 이를 클로드에 복사해서 산출물을 만들게끔 하고 있어
4. 나는 위 페이지를 이용하는 것과, 클로드에게 직접 질문하는 것 두 개
5. 를 병렬적으로 실행하고, 결과물이 더 잘 나온 것을 제출하려고 해.
6. 각각의 폴더에 태스크를 진행하고 결과물을 만들자
7. 3번 링크는 https://techfin-agent-builder.netlify.app/ 야 만약 이를 읽어오기 어려우면 playwright를 통해 파악해도 좋아
8. 내가 만들고 싶은 ai agent 주제는 다음과 같아. 회의록을 텍스트로 일정하지 않게 작성한 상태에서도, 회의에 참여한 부서와 구성원들에게 해야할 업무와 작성해야할 문서, 의사결정이 필요한 부분을 요청하고 진행해야 하는 순서까지 알려주고 공유하게 하고 싶어.
9. 결과물 UI/구조 기준 (아래 사항 반드시 반영):
   - 정보 위계: 서머리 + 크리티컬 패스를 최상단에 강조 표시하고,
     나머지(부서별 실행항목/의사결정 필요/갭·가정/부서별 메시지)는
     접기(아코디언) 또는 탭으로 기본 숨김 처리
   - 크리티컬 패스는 텍스트 나열이 아니라 실제 플로우 다이어그램
     (노드-엣지, 병목 노드는 색상 강조)으로 표현
   - "확인 필요·가정" 섹션은 우선순위 top 3~5개만 기본 노출, 나머지는 더보기 처리
   - 부서별 카드는 데이터 없는 섹션은 렌더링하지 않음 (조건부 렌더링)
10. 진행 방식 원칙:
    - 사소하거나 되돌리기 쉬운 결정(변수명, 파일 구조, 라이브러리 선택 등)은
      스스로 판단해서 진행하고, 어떤 가정을 했는지만 요약해서 보고할 것.
      매번 승인받으러 오지 말 것.
    - 되묻기 전에 먼저: (1) 지시사항.md와 기존 코드/문서를 확인하고,
      (2) 합리적인 기본값(default)을 정해서 진행하고,
      (3) 그 가정을 명시적으로 남길 것.
    - 다음 경우에만 되물을 것:
      a) 되돌리기 어렵거나 비용이 큰 작업 (기존 데이터 삭제, 구조 전면 변경 등)
      b) 지시사항과 실제 결과가 명백히 충돌하는 경우
      c) 여러 선택지가 있고 각각 결과물이 크게 달라지는 경우
    - 질문할 땐 한 번에 몰아서, 선택지 형태(1/2/3)로 물어볼 것.
      "어떻게 할까요?" 식의 열린 질문 대신 구체적 대안 제시.
11. 결과물 품질 기준 (다이어그램/데이터 정확성):
    - 회의록에 날짜/연도가 불명확하면 확정된 사실처럼 표기하지 말고
      항상 "확인 필요" 섹션에 명시할 것
    - "별건, 나중에" 등으로 보류된 항목도 누락하지 말고 
      결과물에 보류 항목으로 표시할 것  
    - 크리티컬 패스 다이어그램은 화살표가 노드를 가로지르거나
      서로 겹치지 않도록 항상 자동 레이아웃/경로 우회 처리할 것
12. 부서 커스터마이징 기능:
    - 회의록 분석 전, 자동으로 감지된 참여 부서 목록을 
      사용자가 미리 확인하고 수정할 수 있게 할 것
      (부서 추가 / 삭제 가능)
    - 결과물이 나온 후에도 부서를 추가하거나 삭제한 뒤,
      변경된 설정으로 회의록을 다시 분석(재실행)할 수 있게 할 것
    - 즉, 사용자가 원하는 방향으로 부서 구성을 
      "분석 전 설정" 또는 "결과 후 수정" 두 시점 모두에서 
      조정 가능해야 함
13. 프로젝트 히스토리 기능:
    - 회의록 입력 전 "프로젝트 선택 또는 새로 만들기" 옵션 표시
    - 같은 프로젝트에 회의록 여러 번 누적 저장 가능
    - 회차별 실행보드를 시간순으로 볼 수 있게
    - 변경된 항목은 색상/태그로 구분 (신규/완료/변경)

14. 회의록 품질 점수:
    - 분석 결과 상단에 점수 표시
    - 담당자 미정 N개, 기한 미정 N개, 의사결정 미완 N개 기준
    - 직관적인 점수 형태로 표현 (예: 60점)
15. 회의록 분석 실행 전, 다음 3가지를 명시적으로 선택하도록 UI에 입력 단계 추가: (1) 회의록 파일, (2) 프로젝트, (3) 회차 번호(숫자). 회차 번호는 파일명에서 자동 추론한 값을 기본값으로 제안하되, 사용자가 직접 수정 가능해야 함.

16. 회차 번호는 "몇 번째로 분석을 실행했는가"가 아니라 사용자가 지정한 고유 식별자로 취급함. 처리 순서와 회차 번호를 절대 동일시하지 않음.

17. 각 분석 결과 저장 시 다음 필드를 포함: 프로젝트 ID, 회차 번호, 회의록 ID, 분석 결과, 처리 시각. 회차 번호 기준으로 조회·정렬 가능해야 함.

18. "이전 회차 대비 변경점" 계산 시 기준(baseline)은 다음 규칙으로 결정: 현재 프로젝트 내 이미 분석 완료된 회차들 중, 회차 번호가 현재 회차보다 작은 것 중 가장 큰 번호를 가진 회차. (처리 순서 무관, 회차 번호로만 판단)
    예: 2 → 5 → 3 → 1 → 4 순서로 분석해도, 5회차는 3회차를, 3회차는 2회차를, 4회차는 3회차를 기준으로 잡아야 함. 1회차는 기준 없음(최소 회차).

19. 특정 회차를 재분석했을 때, 그 회차를 baseline으로 참조 중인 다른 회차(들)가 존재하면, "연쇄 재계산이 필요합니다 (영향 회차: N차, M차) — 지금 다시 계산할까요?" 확인 후 진행. 사용자가 거부하면 해당 회차들에 "⚠ 기준 데이터 오래됨" 표시.

20. 왼쪽 "회차 히스토리" 목록은 처리 순서가 아니라 회차 번호 오름차순으로 정렬해서 표시. 각 항목 옆에 실제 처리 일시를 함께 표기하여 처리 순서와 회차 순서가 다를 수 있음을 시각적으로 구분.
21. 보드 열람 중일 때, 왼쪽 패널에 해당 회차의 회의록 원문을 자동으로 불러와서 표시. 사용자가 직접 내용을 수정할 수 있어야 함.

22. 왼쪽 패널에 "신규 분석" 버튼과 별도로 "업데이트" 버튼을 추가. 업데이트는 현재 회차에 덮어쓰기로 재분석을 실행하며, 새 회차를 생성하지 않음.

23. 업데이트 실행 시 흐름: (1) 수정된 회의록을 Claude가 다시 읽고 분석 → (2) 기존 회차 데이터에 덮어쓰기 → (3) 이 회차를 baseline으로 참조 중인 다른 회차가 있으면 지시사항 19번(연쇄 재계산 확인)과 동일한 흐름으로 처리.

24. 업데이트 완료 후 보드 상단에 "마지막 업데이트: 날짜/시각" 표시. 최초 분석 일시와 구분해서 표기.
25. 그래프/차트 시각적 품질 유지 원칙:
   - 데이터 없을 때 빈 화면 대신 "아직 데이터가 없습니다" 안내 표시
   - 항목 이름이 길어도 잘리지 않게 말줄임 또는 줄바꿈 처리
   - 모바일/작은 화면에서도 깨지지 않게 반응형 처리
   - 색상은 전체 UI 컬러와 통일
   - 차트 타이틀/범례 글씨 크기 최소 12pt 유지
   - 회차가 1개뿐일 때 트렌드 그래프는 숨기고 "2회차부터 트렌드를 확인할 수 있습니다" 안내
26. ## 프론트엔드 UI 개선 공통 원칙

27. 목표 사용자는 금융기관 담당자이며, 전문적인 B2B 데이터 대시보드의 신뢰감과 가독성을 우선하며 UI 개선 시 기존 기능과 사용자 흐름을 유지한다.

28. 별도 요청이 없는 한 다음 항목은 변경하지 않는다.
   - 데이터 로직
   - API 연동
   - 상태 관리
   - 라우팅
   - 데이터 스키마
   - 분석 결과 생성 규칙

29. UI 수정 전 실제 Repository 구조와 파일을 먼저 확인한다.
   - 존재하지 않는 파일명이나 컴포넌트를 추측하지 않는다.
   - 확인하지 못한 내용은 “추가 확인 필요”로 표시한다.

30. 상태 정보는 색상만으로 구분하지 않는다.
   - Badge Text
   - Icon
   - Label
   을 함께 사용한다.

31. 상태 색상 역할은 다음 기준으로 통일한다.
   - Blue: 주요 액션, 선택, 진행 중
   - Green: 완료, 정상
   - Amber: 검토 필요, 기한 임박
   - Red: 지연, 오류, 필수 조치
   - Gray: 미착수, 비활성, 보조 정보

32. 부서 구분은 과도한 색상 사용보다 Label 또는 Icon을 우선한다.

33. 반응형 화면에서도 기능과 데이터는 유지하고 표현 방식만 변경한다.

34. 사용자가 분석만 요청한 경우에는 파일을 수정하거나 커밋하지 않는다.

35. 파일 수정, Git commit, Git push, Vercel 배포는 사용자가 명시적으로 승인한 경우에만 수행한다.