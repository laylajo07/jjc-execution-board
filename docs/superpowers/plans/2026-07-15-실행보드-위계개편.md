# 회의록 실행보드 위계 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 회의록 실행보드에 3단 시각 위계(H1)와 계산으로 검증되는 계층형 DAG 크리티컬 패스를 도입해, "병목이 여기다"가 한눈에 보이게 한다.

**Architecture:** 모델이 적는 것을 **작업·부서·선행 셋**으로 줄이고(`blocks`·`bottleneck`·`step` 제거), 순서·후행·병목·깊이·좌표는 전부 **순수 함수 `buildGraph()`** 가 계산한다. 엔진은 DOM을 만지지 않아 브라우저 없이 `node --test`로 검증된다. 렌더러는 엔진 출력을 그리기만 한다.

**Tech Stack:** 바닐라 JS (ES5 문법, 빌드 없음) · 인라인 SVG · Node 24 내장 `node:test` · Python 3.13 / TypeScript 러너

## Global Constraints

설계 문서 `docs/superpowers/specs/2026-07-15-실행보드-위계개편-design.md` 에서 그대로 옮김. **모든 태스크에 암묵적으로 적용된다.**

- **API 키 0**: `claude -p`(로컬 로그인)만 사용. Anthropic SDK·API 키 도입 금지.
- **설치 0**: `index.html` 더블클릭(`file://`)으로 동작해야 한다. 번들러·npm 의존성 추가 금지. → **ES 모듈(`type="module"`) 금지, 클래식 `<script src>`만 사용** (file:// 에서 모듈은 차단됨).
- **러너 웹루트 = 앱 폴더**: `러너/py/server.py:61` 이 `directory=APP_DIR`. **앱 폴더 밖의 리소스를 참조하면 자동 모드에서 404.** 공유 파일도 각 앱 폴더 안에 둔다.
- **라이트/다크 both** 지원 유지.
- **지어내지 않는다**: 회의록에 없는 담당자·기한·작업을 창작하지 않는다.
- **A의 분석 품질 프롬프트는 불변**: A는 사내 웹페이지가 만든 "A의 두뇌"다. JSON 출력 계약(스키마)만 A·B 동일 적용하고, 분석 규칙(본부 나열 vs 매핑 규칙표, 확인필요 단일 vs 3단계, 날짜 환산 등)은 **손대지 않는다**.
- **A·B 양쪽 적용**: `A_웹페이지/앱/`, `B_직접/앱/` 둘 다.
- 코드 주석·UI 문구는 **한국어**, 기존 톤 유지.

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `B_직접/앱/graph.js` | **그래프 엔진(순수)**. `buildGraph(sequence) → {nodes,edges,warnings,stats}`. DOM 접근 없음 | 신규 |
| `A_웹페이지/앱/graph.js` | 위와 **바이트 단위 동일 사본** (러너 웹루트 제약 때문에 공유 불가) | 신규 |
| `테스트/graph.test.js` | 엔진 단위 테스트 (`node --test`) | 신규 |
| `테스트/fixtures/샘플01_신스키마.json` | 인수 테스트용 신스키마 sequence | 신규 |
| `B_직접/앱/index.html` | 렌더러 + H1 위계. 엔진 결과를 그리기만 | 수정 |
| `A_웹페이지/앱/index.html` | 동일 | 수정 |
| `B_직접/앱/AGENT.md` · `B_직접/시스템프롬프트.md` | B 프롬프트 — 스키마 + 자기검증 | 수정 |
| `A_웹페이지/앱/AGENT.md` · `A_웹페이지/시스템프롬프트.md` | A 프롬프트 — **스키마 블록만** | 수정 |
| `러너/py/server.py` · `러너/ts/server.ts` | `--output-format stream-json --verbose` + 라인 파싱 | 수정 |
| `README.md` · `문서/평가_비교.md` | 틀린 서술 정정 | 수정 |

**왜 `graph.js`가 2벌인가**: 러너가 앱 폴더만 서빙하므로(`directory=APP_DIR`) 상위 공유 폴더는 자동 모드에서 404다. 중복은 나쁘지만 여기선 제약이 강제한다. **Task 6의 동일성 테스트로 드리프트를 막는다.**

---

### Task 0: 안전망 — git 저장소 초기화

이 계획은 40KB짜리 `index.html` 2개를 크게 뜯는다. 되돌릴 수단이 필요하다.

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: git 저장소인지 확인**

Run: `git -C "C:/Users/toyal/Desktop/새 폴더" rev-parse --is-inside-work-tree`
Expected: `fatal: not a git repository` (아니면 이 태스크 전체를 건너뛴다)

- [ ] **Step 2: `.gitignore` 작성**

```gitignore
node_modules/
.superpowers/
러너/ts/node_modules/
```

- [ ] **Step 3: 초기화 + 현재 상태를 통째로 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git init
git add -A
git commit -m "chore: 개편 착수 전 현재 상태 스냅샷"
```

- [ ] **Step 4: 스냅샷 확인**

Run: `git -C "C:/Users/toyal/Desktop/새 폴더" log --oneline -1 && git -C "C:/Users/toyal/Desktop/새 폴더" status --short`
Expected: 커밋 1건, 변경 없음(clean). `node_modules`가 목록에 없어야 함.

---

### Task 1: 러너 스트리밍 수정 + 문서 정정

**왜 먼저 하나**: 이후 태스크에서 `claude -p`를 A·B × 샘플 여러 개로 재실행해야 하는데, 지금은 매번 몇 분씩 **출력 0바이트**로 깜깜이 대기다. 먼저 고치면 남은 작업이 편해진다.

**Files:**
- Modify: `러너/py/server.py:47-56` (`run_claude`), `러너/py/server.py:122-146` (SSE 스트리밍)
- Modify: `러너/ts/server.ts:42-43`, `러너/ts/server.ts:94-95`
- Modify: `README.md`, `문서/평가_비교.md`

**Interfaces:**
- Produces: `collect_text(lines) -> str` (py) — stream-json 라인 배열에서 최종 텍스트를 조립하는 순수 함수. Task 없음(러너 내부용).

**배경 (실측)**: `claude -p`는 `--output-format stream-json --verbose` 없이는 완료까지 아무것도 출력하지 않는다. 실제 프롬프트로 400초 타임아웃까지 출력 0바이트를 확인했다. SSE 배관은 이미 깔려 있고, 흘려보낼 내용이 없었을 뿐이다.

- [ ] **Step 1: stream-json 라인 파서 테스트를 먼저 작성**

Create `테스트/stream_parse_test.py`:

```python
# -*- coding: utf-8 -*-
import sys, os, unittest
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '러너', 'py'))
from server import collect_text

class TestCollectText(unittest.TestCase):
    def test_assistant_텍스트만_이어붙인다(self):
        lines = [
            '{"type":"system","subtype":"init"}',
            '{"type":"assistant","message":{"content":[{"type":"text","text":"안녕"}]}}',
            '{"type":"assistant","message":{"content":[{"type":"text","text":"하세요"}]}}',
            '{"type":"result","subtype":"success"}',
        ]
        self.assertEqual(collect_text(lines), '안녕하세요')

    def test_깨진_라인은_건너뛴다(self):
        lines = ['{쓰레기', '{"type":"assistant","message":{"content":[{"type":"text","text":"ok"}]}}']
        self.assertEqual(collect_text(lines), 'ok')

    def test_빈입력은_빈문자열(self):
        self.assertEqual(collect_text([]), '')

if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && python -m unittest 테스트.stream_parse_test -v`
Expected: FAIL — `ImportError: cannot import name 'collect_text'`

- [ ] **Step 3: `collect_text` 구현**

`러너/py/server.py` 의 `extract_json` 위에 추가:

```python
def collect_text(lines):
    """`claude -p --output-format stream-json` 의 JSON Lines에서 assistant 텍스트만 조립한다."""
    out = []
    for ln in lines:
        ln = (ln or '').strip()
        if not ln:
            continue
        try:
            ev = json.loads(ln)
        except Exception:
            continue  # 깨진 라인은 무시
        if ev.get('type') != 'assistant':
            continue
        for blk in (ev.get('message') or {}).get('content') or []:
            if blk.get('type') == 'text':
                out.append(blk.get('text') or '')
    return ''.join(out)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && python -m unittest 테스트.stream_parse_test -v`
Expected: PASS (3 tests)

- [ ] **Step 5: `run_claude`에 플래그 추가**

`러너/py/server.py:47-56` 을 다음으로 교체:

```python
def run_claude(prompt, model=None):
    args = ['claude', '-p', '--output-format', 'stream-json', '--verbose']
    use = model or MODEL
    if use:
        args += ['--model', use]
    p = subprocess.run(args, input=prompt, capture_output=True, text=True,
                       encoding='utf-8', shell=(os.name == 'nt'), timeout=300)
    raw = collect_text(p.stdout.splitlines())
    if p.returncode != 0 and not raw.strip():
        raise RuntimeError((p.stderr or p.stdout or 'claude 실행 실패').strip())
    return raw
```

- [ ] **Step 6: SSE 스트리밍 경로에 플래그 + 라인 누적 적용**

`러너/py/server.py:122` 의 `args = ['claude', '-p']` 를 교체:

```python
        args = ['claude', '-p', '--output-format', 'stream-json', '--verbose']
```

`러너/py/server.py:134-143` 의 읽기 루프를 교체 (라인 단위로 파싱해 **텍스트만** SSE로 흘린다):

```python
        buf, lines = '', []
        while True:
            b = proc.stdout.read1(4096)
            if not b:
                break
            buf += b.decode('utf-8', 'replace')
            while '\n' in buf:
                ln, buf = buf.split('\n', 1)
                lines.append(ln)
                piece = collect_text([ln])
                if piece:
                    sse({'t': piece})       # 생성되는 텍스트를 실시간 전달
        if buf.strip():
            lines.append(buf)
        proc.wait()
        raw = collect_text(lines)
```

- [ ] **Step 7: TS 러너에 동일 플래그 적용**

`러너/ts/server.ts:42` 와 `러너/ts/server.ts:94` 의 두 곳:

```typescript
    const args = ['-p', '--output-format', 'stream-json', '--verbose'];
```

그리고 각 `spawn` 이후 stdout 처리에서 라인 단위로 `JSON.parse` 하여 `type==='assistant'` 의 `message.content[].text` 만 이어붙인다:

```typescript
function collectText(line: string): string {
  try {
    const ev = JSON.parse(line);
    if (ev?.type !== 'assistant') return '';
    return (ev.message?.content ?? [])
      .filter((b: any) => b?.type === 'text')
      .map((b: any) => b.text ?? '')
      .join('');
  } catch { return ''; }
}
```

- [ ] **Step 8: 실제 스트리밍 동작 확인 (증거 필수)**

```bash
cd "C:/Users/toyal/Desktop/새 폴더/러너/py"
python -u server.py ../../B_직접/앱 8787 &
curl -N -s -X POST http://localhost:8787/api/analyze \
  -H 'Content-Type: application/json' \
  -d '{"note":"7/14 CB본부 김민정: 리포트 7/20까지 작성."}' | head -20
```

Expected: **완료 전에** `data: {"t":"..."}` 조각이 순차적으로 도착. 마지막에 `data: {"done":true,...}`.
❗ 조각 없이 마지막에 한 번에 쏟아지면 실패 — 플래그가 안 먹은 것이다.

- [ ] **Step 9: 문서의 틀린 서술 정정**

`README.md` — 아래 문장을 찾아 교체:

- 기존: `⏱ \`claude -p\`는 매 요청 세션을 새로 부팅해 **모델과 무관하게 1~3분** 걸립니다. 반복 사용은 복붙 모드가 빠릅니다.`
- 교체: `⏱ \`claude -p\` 소요 시간은 **출력 길이에 비례**합니다(짧은 프롬프트는 5초, 실행보드 전체 생성은 수 분). 생성 과정은 자동 모드에서 실시간으로 표시됩니다.`

`문서/평가_비교.md` — 6절 검증 로그의 스트리밍 항목을 교체:

- 기존: `- **결과 스트리밍(SSE)**: 러너가 \`claude -p\` 출력을 실시간 전달, 웹앱이 생성 과정을 표시 후 보드 렌더링.`
- 교체: `- **결과 스트리밍(SSE)**: \`--output-format stream-json\` 으로 \`claude -p\` 출력을 라인 단위 수신해 실시간 전달. (2026-07-15 이전 버전은 이 플래그가 없어 완료 시까지 출력이 0바이트였고, "실시간 표시" 안내가 사실과 달랐음 — 이번에 수정.)`

`문서/평가_비교.md` — "⏱ 운영 관찰" 절의 `모델과 무관하게 1~3분` 서술도 같은 취지로 정정한다.

- [ ] **Step 10: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add 러너/py/server.py 러너/ts/server.ts 테스트/stream_parse_test.py README.md 문서/평가_비교.md
git commit -m "fix(러너): claude -p 스트리밍 활성화 + 문서의 틀린 지연/스트리밍 서술 정정"
```

---

### Task 2: 그래프 엔진 코어 — id 모드

**Files:**
- Create: `B_직접/앱/graph.js`
- Create: `테스트/graph.test.js`

**Interfaces:**
- Produces:
  - `buildGraph(sequence: Array) -> {nodes, edges, warnings, stats}`
  - `nodes[i]` = `{id, task, dept, depth, downstream, isBottleneck, externals: string[], x, y}`
  - `edges[i]` = `{from: id, to: id, critical: boolean}`
  - `warnings[i]` = `{type: 'empty'|'single'|'cycle'|'unresolved', detail: string}`
  - `stats` = `{edgeTotal, edgeResolved, mode: 'id'|'fuzzy'}`
  - 상수 `NODE_W=170, NODE_H=48, GAP_X=60, GAP_Y=14`
  - 브라우저에서는 `window.Graph`, Node에서는 `module.exports`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `테스트/graph.test.js`:

```js
const test = require('node:test');
const assert = require('node:assert');
const { buildGraph } = require('../B_직접/앱/graph.js');

const node = (g, id) => g.nodes.find(n => n.id === id);

test('선행(blocked_by)에서 후행·깊이·병목을 파생한다', () => {
  const g = buildGraph([
    { id: 'T1', task: '재학습', dept: 'CB본부', blocked_by: [] },
    { id: 'T2', task: 'API 최적화', dept: 'ICT본부', blocked_by: ['T1'] },
    { id: 'T3', task: 'PoC 시연', dept: '사업솔루션본부', blocked_by: ['T2'] },
  ]);
  assert.equal(g.stats.mode, 'id');
  assert.equal(g.stats.edgeTotal, 2);
  assert.equal(g.stats.edgeResolved, 2);
  assert.equal(node(g, 'T1').depth, 0);
  assert.equal(node(g, 'T2').depth, 1);
  assert.equal(node(g, 'T3').depth, 2);
  assert.equal(node(g, 'T1').downstream, 2, 'T1은 T2·T3 둘을 막는다');
  assert.equal(node(g, 'T3').downstream, 0);
  assert.equal(node(g, 'T1').isBottleneck, true);
  assert.equal(node(g, 'T2').isBottleneck, false);
});

test('깊이는 최단이 아니라 최장 경로다 — 모든 선행보다 뒤에 놓인다', () => {
  // T1→T3, T1→T2→T3 : T3는 depth 1이 아니라 2여야 한다
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [] },
    { id: 'T2', task: 'b', dept: '', blocked_by: ['T1'] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T1', 'T2'] },
  ]);
  assert.equal(node(g, 'T3').depth, 2);
});

test('병목 동점이면 둘 다 표시한다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [] },
    { id: 'T2', task: 'b', dept: '', blocked_by: [] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T1'] },
    { id: 'T4', task: 'd', dept: '', blocked_by: ['T2'] },
  ]);
  assert.equal(node(g, 'T1').isBottleneck, true);
  assert.equal(node(g, 'T2').isBottleneck, true);
});

test('병목에서 나가는 엣지만 critical이다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [] },
    { id: 'T2', task: 'b', dept: '', blocked_by: ['T1'] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T2'] },
  ]);
  assert.equal(g.edges.find(e => e.from === 'T1').critical, true);
  assert.equal(g.edges.find(e => e.from === 'T2').critical, false);
});

test('blocked_by_external은 엣지가 아니라 노드의 externals로 보존된다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [], blocked_by_external: ['제품 스펙 확정'] },
  ]);
  assert.deepEqual(node(g, 'T1').externals, ['제품 스펙 확정']);
  assert.equal(g.edges.length, 0);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: FAIL — `Cannot find module '../B_직접/앱/graph.js'`

- [ ] **Step 3: `B_직접/앱/graph.js` 구현 (코어)**

```js
/* 조정치 · 그래프 엔진 (순수 함수 — DOM 접근 없음)
   모델은 작업·부서·선행만 적는다. 순서·후행·병목·깊이·좌표는 전부 여기서 계산한다.
   file:// 더블클릭을 지원해야 하므로 ES 모듈이 아닌 클래식 스크립트다. */
(function (root) {
  'use strict';

  var NODE_W = 170, NODE_H = 48, GAP_X = 60, GAP_Y = 14;

  function makeNodes(seq) {
    return seq.map(function (s, i) {
      return {
        id: (s && s.id) ? String(s.id) : 'T' + (i + 1),
        task: String((s && s.task) || ''),
        dept: String((s && s.dept) || ''),
        externals: ((s && s.blocked_by_external) || []).map(String),
        depth: 0, downstream: 0, isBottleneck: false, x: 0, y: 0,
        _i: i
      };
    });
  }

  // 선행 목록 → 엣지. 해석 실패한 것은 externals로 강등한다.
  function buildEdges(seq, nodes, index) {
    var edges = [], total = 0, resolved = 0;
    seq.forEach(function (s, i) {
      var self = nodes[i];
      ((s && s.blocked_by) || []).forEach(function (b) {
        total++;
        var from = String(b);
        if (index[from] && from !== self.id) {
          edges.push({ from: from, to: self.id, critical: false });
          resolved++;
        } else {
          self.externals.push(from);   // 없는 id를 가리킴 → 정직하게 external로
        }
      });
    });
    return { edges: edges, total: total, resolved: resolved };
  }

  function predMap(nodes, edges) {
    var m = {};
    nodes.forEach(function (n) { m[n.id] = []; });
    edges.forEach(function (e) { m[e.to].push(e.from); });
    return m;
  }

  function succMap(nodes, edges) {
    var m = {};
    nodes.forEach(function (n) { m[n.id] = []; });
    edges.forEach(function (e) { m[e.from].push(e.to); });
    return m;
  }

  // 최장 경로 깊이 — 모든 선행보다 반드시 뒤에 놓이게 한다.
  function computeDepth(nodes, preds) {
    var depth = {}, busy = {};
    function d(id) {
      if (depth[id] != null) return depth[id];
      busy[id] = true;
      var m = 0;
      preds[id].forEach(function (p) {
        if (!busy[p]) m = Math.max(m, d(p) + 1);
      });
      busy[id] = false;
      depth[id] = m;
      return m;
    }
    nodes.forEach(function (n) { n.depth = d(n.id); });
  }

  // 도달 가능한 후행 집합의 크기 = 이 작업이 막고 있는 작업 수
  function computeDownstream(nodes, succ) {
    var memo = {};
    function reach(id) {
      if (memo[id]) return memo[id];
      var set = {};
      memo[id] = set;
      succ[id].forEach(function (t) {
        set[t] = true;
        var sub = reach(t);
        for (var k in sub) if (Object.prototype.hasOwnProperty.call(sub, k)) set[k] = true;
      });
      return set;
    }
    nodes.forEach(function (n) { n.downstream = Object.keys(reach(n.id)).length; });
  }

  function markBottleneck(nodes, edges) {
    var max = 0;
    nodes.forEach(function (n) { if (n.downstream > max) max = n.downstream; });
    if (max <= 0) return;
    nodes.forEach(function (n) { n.isBottleneck = (n.downstream === max); });
    var hot = {};
    nodes.forEach(function (n) { if (n.isBottleneck) hot[n.id] = true; });
    edges.forEach(function (e) { e.critical = !!hot[e.from]; });
  }

  function buildGraph(sequence) {
    var seq = Array.isArray(sequence) ? sequence : [];
    if (!seq.length) {
      return {
        nodes: [], edges: [],
        warnings: [{ type: 'empty', detail: '진행 순서 데이터가 없습니다' }],
        stats: { edgeTotal: 0, edgeResolved: 0, mode: 'id' }
      };
    }

    var nodes = makeNodes(seq);
    var index = {};
    nodes.forEach(function (n) { index[n.id] = n; });

    var built = buildEdges(seq, nodes, index);
    var edges = built.edges;

    var preds = predMap(nodes, edges);
    var succ = succMap(nodes, edges);
    computeDepth(nodes, preds);
    computeDownstream(nodes, succ);
    markBottleneck(nodes, edges);

    return {
      nodes: nodes, edges: edges, warnings: [],
      stats: { edgeTotal: built.total, edgeResolved: built.resolved, mode: 'id' }
    };
  }

  var api = {
    buildGraph: buildGraph,
    NODE_W: NODE_W, NODE_H: NODE_H, GAP_X: GAP_X, GAP_Y: GAP_Y
  };

  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else root.Graph = api;
})(typeof globalThis !== 'undefined' ? globalThis : this);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add B_직접/앱/graph.js 테스트/graph.test.js
git commit -m "feat(엔진): 그래프 코어 — 선행에서 후행·깊이·병목 파생"
```

---

### Task 3: 그래프 엔진 — 순환 의존 · 엣지 케이스

**Files:**
- Modify: `B_직접/앱/graph.js`
- Modify: `테스트/graph.test.js`

**Interfaces:**
- Consumes: Task 2의 `buildGraph`
- Produces: `warnings` 에 `{type:'cycle', detail:'T1→T2→T1'}`, `{type:'single'}`, `{type:'unresolved', detail}` 추가

**원칙**: 순환은 **절대 조용히 버리지 않는다.** 레이아웃을 위해 back edge만 빼고, 경고로 반드시 알린다.

- [ ] **Step 1: 실패하는 테스트 추가**

`테스트/graph.test.js` 끝에 추가:

```js
test('순환 의존을 탐지하고 경고한다 — 조용히 버리지 않는다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: ['T3'] },
    { id: 'T2', task: 'b', dept: '', blocked_by: ['T1'] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T2'] },
  ]);
  const cyc = g.warnings.filter(w => w.type === 'cycle');
  assert.equal(cyc.length, 1, '순환 경고가 있어야 한다');
  assert.match(cyc[0].detail, /T1|T2|T3/);
  // back edge를 뺐으므로 레이아웃은 유한해야 한다
  assert.ok(g.nodes.every(n => Number.isFinite(n.depth)));
});

test('자기 자신을 선행으로 적으면 엣지가 되지 않는다', () => {
  const g = buildGraph([{ id: 'T1', task: 'a', dept: '', blocked_by: ['T1'] }]);
  assert.equal(g.edges.length, 0);
  assert.deepEqual(g.nodes[0].externals, ['T1']);
});

test('없는 id를 선행으로 적으면 external로 강등하고 경고한다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: ['없는거'] },
  ]);
  assert.equal(g.edges.length, 0);
  assert.deepEqual(g.nodes[0].externals, ['없는거']);
  assert.equal(g.stats.edgeTotal, 1);
  assert.equal(g.stats.edgeResolved, 0);
  assert.ok(g.warnings.some(w => w.type === 'unresolved'));
});

test('빈 입력', () => {
  const g = buildGraph([]);
  assert.deepEqual(g.nodes, []);
  assert.ok(g.warnings.some(w => w.type === 'empty'));
});

test('노드 1개면 single 경고 — 다이어그램 대신 한 줄로 표시하게 한다', () => {
  const g = buildGraph([{ id: 'T1', task: 'a', dept: '', blocked_by: [] }]);
  assert.ok(g.warnings.some(w => w.type === 'single'));
});

test('id가 없으면 배열 순서로 T1..Tn을 자동 부여한다', () => {
  const g = buildGraph([
    { task: 'a', dept: '', blocked_by: [] },
    { task: 'b', dept: '', blocked_by: [] },
  ]);
  assert.deepEqual(g.nodes.map(n => n.id), ['T1', 'T2']);
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: FAIL — cycle/single/unresolved 경고 없음

- [ ] **Step 3: 순환 탐지 + 경고 구현**

`graph.js` 의 `predMap` 위에 추가:

```js
  // DFS로 back edge를 찾아 제거한다. 제거한 것은 반드시 경고로 알린다.
  function detectCycles(nodes, edges) {
    var adj = {}, state = {}, backs = [], cycles = [], stack = [];
    nodes.forEach(function (n) { adj[n.id] = []; state[n.id] = 0; });
    edges.forEach(function (e) { adj[e.from].push(e); });

    function dfs(id) {
      state[id] = 1; stack.push(id);
      adj[id].forEach(function (e) {
        if (state[e.to] === 1) {
          backs.push(e);
          var at = stack.indexOf(e.to);
          cycles.push(stack.slice(at).concat(e.to).join('→'));
        } else if (state[e.to] === 0) {
          dfs(e.to);
        }
      });
      stack.pop(); state[id] = 2;
    }
    nodes.forEach(function (n) { if (state[n.id] === 0) dfs(n.id); });

    var kept = edges.filter(function (e) { return backs.indexOf(e) === -1; });
    return { edges: kept, cycles: cycles };
  }
```

`buildGraph` 의 `var edges = built.edges;` 다음을 교체:

```js
    var warnings = [];

    var cyc = detectCycles(nodes, built.edges);
    var edges = cyc.edges;
    cyc.cycles.forEach(function (c) {
      warnings.push({ type: 'cycle', detail: c });
    });

    var unresolved = built.total - built.resolved;
    if (unresolved > 0) {
      warnings.push({
        type: 'unresolved',
        detail: '선행 ' + built.total + '개 중 ' + unresolved + '개를 작업 목록에서 찾지 못해 별도 표시합니다'
      });
    }
    if (nodes.length === 1) {
      warnings.push({ type: 'single', detail: '작업이 하나뿐이라 의존 관계가 없습니다' });
    }
```

그리고 `return` 문의 `warnings: []` 를 `warnings: warnings` 로 교체.

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (11 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add B_직접/앱/graph.js 테스트/graph.test.js
git commit -m "feat(엔진): 순환 의존 탐지 + 엣지 케이스 경고"
```

---

### Task 4: 그래프 엔진 — 구버전 fuzzy fallback

**Files:**
- Modify: `B_직접/앱/graph.js`
- Modify: `테스트/graph.test.js`

**Interfaces:**
- Consumes: Task 3의 `buildGraph`
- Produces: `stats.mode === 'fuzzy'` 경로. id 필드가 **하나도 없을 때만** 발동.

**핵심**: fuzzy는 **완전 복구가 아니다.** 8개 중 일부만 살아나는 게 정상이고 의도된 동작이다. 애매하면 연결하지 않는다 — 조용히 그럴듯한 화살표를 그리는 게 최악이다.

**측정 근거** (설계 4-4):
- `'v2 모델 확정'` vs `'v2 모델 재학습 → AUC 0.85 달성'` — Jaccard 0.19(실패) / 포함계수 0.60(성공) → **포함 계수를 쓴다**
- `'v2 모델 성능 확정'` — 포함계수 0.43 → 연결 안 됨. **정상**
- `'제품 스펙 확정'` — 대응 노드 없음 → external. **정상**

- [ ] **Step 1: 실패하는 테스트 추가 (실제 구버전 파일 사용)**

`테스트/graph.test.js` 끝에 추가:

```js
const fs = require('node:fs');
const path = require('node:path');

test('구버전(id 없음) 데이터는 fuzzy 모드로 떨어진다', () => {
  const g = buildGraph([
    { task: 'v2 모델 재학습 → AUC 0.85 달성', dept: 'CB본부', blocked_by: [] },
    { task: 'API 응답속도 최적화·배포', dept: 'ICT본부', blocked_by: ['v2 모델 확정'] },
  ]);
  assert.equal(g.stats.mode, 'fuzzy');
  assert.equal(g.stats.edgeResolved, 1, '짧은 별칭이 긴 정식명칭에 붙어야 한다');
  assert.equal(g.edges[0].from, 'T1');
  assert.equal(g.edges[0].to, 'T2');
});

test('접미 차이만 있는 것은 붙는다', () => {
  const g = buildGraph([
    { task: '가격 단가표 초안 작성', dept: '', blocked_by: [] },
    { task: '가격정책 확정', dept: '', blocked_by: ['가격 단가표 초안'] },
  ]);
  assert.equal(g.stats.edgeResolved, 1);
});

test('대응 노드가 없으면 external로 두고 지어내지 않는다', () => {
  const g = buildGraph([
    { task: 'v2 모델 재학습 → AUC 0.85 달성', dept: '', blocked_by: [] },
    { task: '마케팅 브로셔·랜딩 제작', dept: '', blocked_by: ['제품 스펙 확정'] },
  ]);
  assert.equal(g.stats.edgeResolved, 0);
  assert.deepEqual(g.nodes[1].externals, ['제품 스펙 확정']);
});

test('실제 구버전 샘플01 파일이 크래시 없이 처리되고 성능 저하가 stats에 드러난다', () => {
  const p = path.join(__dirname, '..', 'B_직접', '앱', '결과', '샘플01_결과.json');
  const d = JSON.parse(fs.readFileSync(p, 'utf8'));
  const g = buildGraph(d.sequence);
  assert.equal(g.stats.mode, 'fuzzy');
  assert.equal(g.stats.edgeTotal, 8, '구버전 샘플01의 선행은 8개');
  assert.ok(g.stats.edgeResolved < g.stats.edgeTotal, 'fuzzy는 완전 복구가 아니다');
  assert.ok(g.warnings.some(w => w.type === 'unresolved'), '몇 개가 안 붙었는지 반드시 알려야 한다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: FAIL — `mode`가 항상 `'id'`, `edgeResolved`가 0

- [ ] **Step 3: fuzzy 매칭 구현**

`graph.js` 의 `makeNodes` 위에 추가:

```js
  var SUFFIX = /(작성|확정|준비|검토|완료|진행)$/;

  function normalize(s) {
    var n = String(s == null ? '' : s).toLowerCase()
      .replace(/[\s·→\-—()[\]{}.,/'"`:;!?~]/g, '');
    return n.replace(SUFFIX, '');
  }

  function bigrams(s) {
    var n = normalize(s), set = {}, i;
    if (n.length < 2) { if (n) set[n] = true; return set; }
    for (i = 0; i < n.length - 1; i++) set[n.slice(i, i + 2)] = true;
    return set;
  }

  // 포함 계수(overlap coefficient). Jaccard가 아닌 이유: 짧은 별칭이 긴 정식명칭을
  // 가리키는 게 이 데이터의 지배적 패턴이라, 길이 차이에 벌점을 주면 안 된다.
  function containment(a, b) {
    var A = bigrams(a), B = bigrams(b);
    var ka = Object.keys(A), kb = Object.keys(B);
    if (!ka.length || !kb.length) return 0;
    var inter = 0;
    ka.forEach(function (k) { if (B[k]) inter++; });
    return inter / Math.min(ka.length, kb.length);
  }

  // 애매하면 연결하지 않는다. 틀린 화살표가 없는 화살표보다 나쁘다.
  function resolveFuzzy(label, nodes, selfId) {
    var scored = nodes
      .filter(function (n) { return n.id !== selfId; })
      .map(function (n) { return { id: n.id, score: containment(label, n.task) }; })
      .sort(function (x, y) { return y.score - x.score; });
    if (!scored.length) return null;
    var best = scored[0], second = scored[1];
    if (best.score < 0.5) return null;
    if (second && (best.score - second.score) < 0.15) return null;
    return best.id;
  }
```

`buildEdges` 를 모드 인자를 받도록 교체:

```js
  function buildEdges(seq, nodes, index, mode) {
    var edges = [], total = 0, resolved = 0;
    seq.forEach(function (s, i) {
      var self = nodes[i];
      ((s && s.blocked_by) || []).forEach(function (b) {
        total++;
        var label = String(b);
        var from = (mode === 'id') ? label : resolveFuzzy(label, nodes, self.id);
        if (from && index[from] && from !== self.id) {
          edges.push({ from: from, to: self.id, critical: false });
          resolved++;
        } else {
          self.externals.push(label);
        }
      });
    });
    return { edges: edges, total: total, resolved: resolved };
  }
```

`buildGraph` 안, `var nodes = makeNodes(seq);` 앞에 모드 판정을 추가:

```js
    // id 필드가 하나라도 있으면 신스키마로 간주한다.
    var mode = seq.some(function (s) { return s && s.id; }) ? 'id' : 'fuzzy';
```

그리고 `buildEdges(seq, nodes, index)` → `buildEdges(seq, nodes, index, mode)`,
`stats` 의 `mode: 'id'` → `mode: mode` 로 교체.

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (15 tests)

- [ ] **Step 5: 실제 구버전 샘플이 몇 개나 붙는지 기록**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
node -e "const {buildGraph}=require('./B_직접/앱/graph.js');const d=require('./B_직접/앱/결과/샘플01_결과.json');const g=buildGraph(d.sequence);console.log('mode:',g.stats.mode,'| 연결:',g.stats.edgeResolved+'/'+g.stats.edgeTotal);console.log('병목:',g.nodes.filter(n=>n.isBottleneck).map(n=>n.task+'(후행 '+n.downstream+')'));"
```

Expected: `mode: fuzzy | 연결: N/8` (N < 8). 이 수치를 Task 10의 고지 문구 검증에 쓴다.

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add B_직접/앱/graph.js 테스트/graph.test.js
git commit -m "feat(엔진): 구버전 데이터 fuzzy fallback (포함계수 기반, 애매하면 미연결)"
```

---

### Task 5: 그래프 엔진 — 좌표 레이아웃 + 샘플01 인수 테스트

**Files:**
- Modify: `B_직접/앱/graph.js`
- Modify: `테스트/graph.test.js`
- Create: `테스트/fixtures/샘플01_신스키마.json`

**Interfaces:**
- Consumes: Task 4의 `buildGraph`
- Produces: 모든 `node.x`, `node.y` 가 채워진다. `x = depth * (NODE_W + GAP_X)`.

- [ ] **Step 1: 인수 테스트용 신스키마 fixture 작성**

Create `테스트/fixtures/샘플01_신스키마.json`:

```json
{
  "sequence": [
    { "id": "T1", "task": "대체데이터 개인정보 검토의견 작성", "dept": "법무실", "blocked_by": [], "blocked_by_external": [] },
    { "id": "T2", "task": "v2 모델 재학습 → AUC 0.85 달성", "dept": "CB본부", "blocked_by": [], "blocked_by_external": [] },
    { "id": "T3", "task": "가격 단가표 초안 작성", "dept": "경영본부", "blocked_by": [], "blocked_by_external": [] },
    { "id": "T4", "task": "가격정책 확정", "dept": "경영본부", "blocked_by": ["T3"], "blocked_by_external": [] },
    { "id": "T5", "task": "API 응답속도 최적화·배포", "dept": "ICT본부", "blocked_by": ["T2"], "blocked_by_external": [] },
    { "id": "T6", "task": "PoC 데모환경·샘플리포트 준비", "dept": "경영본부/ICT본부", "blocked_by": ["T2", "T5"], "blocked_by_external": [] },
    { "id": "T7", "task": "이사회 보고자료 초안(7/18)", "dept": "경영본부", "blocked_by": ["T2", "T4"], "blocked_by_external": [] },
    { "id": "T8", "task": "저축은행 3곳 PoC 시연(8/15)", "dept": "사업솔루션본부", "blocked_by": ["T6"], "blocked_by_external": [] },
    { "id": "T9", "task": "마케팅 브로셔·랜딩 제작", "dept": "마케팅본부", "blocked_by": [], "blocked_by_external": ["제품 스펙 확정"] }
  ]
}
```

- [ ] **Step 2: 실패하는 테스트 추가**

`테스트/graph.test.js` 끝에 추가:

```js
const { NODE_W, GAP_X } = require('../B_직접/앱/graph.js');

test('좌표: x는 깊이에 비례하고, 같은 열은 y가 겹치지 않는다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [] },
    { id: 'T2', task: 'b', dept: '', blocked_by: [] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T1'] },
  ]);
  assert.equal(node(g, 'T1').x, 0);
  assert.equal(node(g, 'T3').x, NODE_W + GAP_X);
  assert.notEqual(node(g, 'T1').y, node(g, 'T2').y, '같은 열의 두 노드는 y가 달라야 한다');
});

test('좌표는 결정적이다 — 같은 입력이면 같은 출력', () => {
  const input = [
    { id: 'T1', task: 'a', dept: '', blocked_by: [] },
    { id: 'T2', task: 'b', dept: '', blocked_by: ['T1'] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T1'] },
  ];
  const a = buildGraph(JSON.parse(JSON.stringify(input)));
  const b = buildGraph(JSON.parse(JSON.stringify(input)));
  assert.deepEqual(a.nodes.map(n => [n.id, n.x, n.y]), b.nodes.map(n => [n.id, n.x, n.y]));
});

// ★ 설계 8절의 인수 기준
test('[인수] 샘플01 신스키마: 엣지 8/8 · 병목 = v2 재학습 · 후행 4', () => {
  const d = JSON.parse(fs.readFileSync(path.join(__dirname, 'fixtures', '샘플01_신스키마.json'), 'utf8'));
  const g = buildGraph(d.sequence);

  assert.equal(g.stats.mode, 'id');
  assert.equal(g.stats.edgeTotal, 8);
  assert.equal(g.stats.edgeResolved, 8, '신스키마는 선행이 전부 연결되어야 한다');
  assert.equal(g.warnings.filter(w => w.type === 'unresolved').length, 0);
  assert.equal(g.warnings.filter(w => w.type === 'cycle').length, 0);

  const bn = g.nodes.filter(n => n.isBottleneck);
  assert.equal(bn.length, 1);
  assert.equal(bn[0].id, 'T2');
  assert.equal(bn[0].task, 'v2 모델 재학습 → AUC 0.85 달성');
  assert.equal(bn[0].downstream, 4, 'T5·T6·T7·T8 넷을 막는다');

  // 끊긴 선행은 지어내지 않고 external로 보존
  assert.deepEqual(node(g, 'T9').externals, ['제품 스펙 확정']);
});
```

- [ ] **Step 3: 실패 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: FAIL — `x`/`y`가 전부 0이라 `notEqual(y, y)` 실패

- [ ] **Step 4: 레이아웃 구현**

`graph.js` 의 `markBottleneck` 아래에 추가:

```js
  // 열 = 깊이. 열 안 순서는 선행들의 슬롯 평균(barycenter)으로 정해 교차를 줄인다.
  // 동점은 원본 배열 순서로 깨서 항상 같은 결과가 나오게 한다.
  function layout(nodes, preds) {
    var byDepth = {};
    nodes.forEach(function (n) {
      (byDepth[n.depth] = byDepth[n.depth] || []).push(n);
    });
    var slot = {};

    function bary(n) {
      var ps = preds[n.id].filter(function (p) { return slot[p] != null; });
      if (!ps.length) return n._i;
      var s = 0;
      ps.forEach(function (p) { s += slot[p]; });
      return s / ps.length;
    }

    Object.keys(byDepth).map(Number).sort(function (a, b) { return a - b; })
      .forEach(function (d) {
        var col = byDepth[d];
        col.sort(function (a, b) {
          var ba = bary(a), bb = bary(b);
          if (ba !== bb) return ba - bb;
          return a._i - b._i;
        });
        col.forEach(function (n, i) {
          slot[n.id] = i;
          n.x = d * (NODE_W + GAP_X);
          n.y = i * (NODE_H + GAP_Y);
        });
      });
  }
```

`buildGraph` 의 `markBottleneck(nodes, edges);` 다음 줄에 추가:

```js
    layout(nodes, preds);
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (18 tests). **인수 테스트가 반드시 통과해야 한다.**

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add B_직접/앱/graph.js 테스트/graph.test.js 테스트/fixtures/샘플01_신스키마.json
git commit -m "feat(엔진): 좌표 레이아웃 + 샘플01 인수 테스트(엣지 8/8·병목 v2·후행 4)"
```

---

### Task 6: `graph.js` 를 A·B 양쪽에 배치 + 동일성 테스트

**Files:**
- Create: `A_웹페이지/앱/graph.js` (B의 사본)
- Modify: `테스트/graph.test.js`
- Modify: `B_직접/앱/index.html`, `A_웹페이지/앱/index.html` — `<script src="graph.js">` 추가

- [ ] **Step 1: 동일성 테스트를 먼저 작성 (실패해야 함)**

`테스트/graph.test.js` 끝에 추가:

```js
test('A·B의 graph.js는 바이트 단위로 동일해야 한다 (복사 누락 방지)', () => {
  const a = fs.readFileSync(path.join(__dirname, '..', 'A_웹페이지', '앱', 'graph.js'), 'utf8');
  const b = fs.readFileSync(path.join(__dirname, '..', 'B_직접', '앱', 'graph.js'), 'utf8');
  assert.equal(a, b, 'A와 B의 graph.js가 다릅니다 — 한쪽만 고쳤습니다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: FAIL — `ENOENT: A_웹페이지/앱/graph.js`

- [ ] **Step 3: 복사**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
cp B_직접/앱/graph.js A_웹페이지/앱/graph.js
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (19 tests)

- [ ] **Step 5: 두 index.html의 `</head>` 직전에 클래식 스크립트 추가**

A·B 각각의 `index.html` 에서 `</head>` 바로 위에 삽입:

```html
<script src="graph.js"></script>
```

❗ `type="module"` 을 쓰면 **file:// 더블클릭이 깨진다.** 반드시 클래식 스크립트여야 한다.

- [ ] **Step 6: file:// 더블클릭에서 엔진이 로드되는지 확인 (증거 필수)**

`B_직접/앱/index.html` 을 **탐색기에서 더블클릭**해 브라우저로 연 뒤, 개발자도구(F12) 콘솔에 입력:

```js
Graph.buildGraph([{id:'T1',task:'a',dept:'',blocked_by:[]},{id:'T2',task:'b',dept:'',blocked_by:['T1']}]).nodes.map(n=>[n.id,n.depth,n.downstream])
```

Expected: `[['T1',0,1],['T2',1,0]]`
❗ `Graph is not defined` 가 나오면 file:// 에서 스크립트가 차단된 것이다. 그 경우 **엔진을 index.html 안에 인라인**하고, 테스트는 `테스트/graph.test.js` 가 참조할 별도 사본을 유지하는 방식으로 계획을 수정해야 한다(이 단계 전에는 진행 금지).

- [ ] **Step 7: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add A_웹페이지/앱/graph.js B_직접/앱/index.html A_웹페이지/앱/index.html 테스트/graph.test.js
git commit -m "feat: A·B 앱에 그래프 엔진 배치 + 동일성 테스트"
```

---

### Task 7: 계층형 DAG 렌더러 (`dag-view.js`)

**Files:**
- Create: `B_직접/앱/dag-view.js`, `A_웹페이지/앱/dag-view.js` (바이트 동일 사본)
- Modify: `테스트/graph.test.js`
- Modify: `B_직접/앱/index.html` (기존 `.rail` 렌더링부 — `index.html:545-551`), `A_웹페이지/앱/index.html`

**왜 별도 파일인가**: `renderDag(g) -> SVG 문자열` 은 DOM을 만지지 않는 **순수 함수**다. 별도 파일로 두면 (1) `node --test`로 검증되고 (2) A·B에 손으로 복붙하지 않아 드리프트가 없다(Task 6과 같은 동일성 테스트로 강제).

**Interfaces:**
- Consumes: `Graph.buildGraph(sequence)` → `{nodes, edges, warnings, stats}`, `Graph.NODE_W/NODE_H/GAP_X/GAP_Y`
- Produces:
  - `DagView.renderDag(g) -> string` (SVG 문자열)
  - `DagView.renderDagWarnings(g) -> string` (경고 HTML)
  - 브라우저에서는 `window.DagView` (반드시 `graph.js` **다음에** 로드), Node에서는 `module.exports`

**시각 규칙** (확정된 A안):
- 열 = 의존 깊이. 열 머리에 `1단계 · 지금 시작 가능` 등 라벨
- 부서 = 노드 좌측 색 바. 병목 = 빨간 굵은 테두리 + `⚠ 후행 N개가 이걸 기다림`
- **병목에서 나가는 엣지만 빨강**(`edge.critical`), 나머지는 회색
- `externals` 는 점선 스텁 + `? 선행 '<라벨>' 노드 없음`
- `warnings` 는 다이어그램 위 경고 줄로 표시. `unresolved` 는 `stats.edgeResolved/edgeTotal` 를 함께 노출

- [ ] **Step 1: 실패하는 테스트를 먼저 작성**

`테스트/graph.test.js` 끝에 추가:

```js
const { renderDag, renderDagWarnings } = require('../B_직접/앱/dag-view.js');

test('DAG SVG: 병목 노드에 후행 개수 배지가 붙는다', () => {
  const g = buildGraph([
    { id: 'T1', task: '재학습', dept: 'CB본부', blocked_by: [] },
    { id: 'T2', task: 'API', dept: 'ICT본부', blocked_by: ['T1'] },
    { id: 'T3', task: 'PoC', dept: '사업솔루션본부', blocked_by: ['T2'] },
  ]);
  const svg = renderDag(g);
  assert.match(svg, /^<svg /);
  assert.match(svg, /후행 2개가 이걸 기다림/);
});

test('병목에서 나가는 엣지만 crit 클래스를 갖는다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [] },
    { id: 'T2', task: 'b', dept: '', blocked_by: ['T1'] },
    { id: 'T3', task: 'c', dept: '', blocked_by: ['T2'] },
  ]);
  const svg = renderDag(g);
  assert.equal((svg.match(/class="dag-e crit"/g) || []).length, 1);
  assert.equal((svg.match(/class="dag-e"/g) || []).length, 1);
});

test('external 선행은 점선 스텁으로 그려진다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: [], blocked_by_external: ['제품 스펙 확정'] },
  ]);
  assert.match(renderDag(g), /dag-x/);
  assert.match(renderDag(g), /제품 스펙/);
});

test('XSS: 회의록에서 온 문자열은 이스케이프된다', () => {
  const g = buildGraph([
    { id: 'T1', task: '<script>alert(1)</script>', dept: '"><img>', blocked_by: [] },
  ]);
  const svg = renderDag(g);
  assert.ok(!svg.includes('<script>'), '스크립트 태그가 그대로 들어가면 안 된다');
  assert.match(svg, /&lt;script&gt;/);
});

test('fuzzy 모드면 몇 개가 연결됐는지 고지한다', () => {
  const g = buildGraph([
    { task: 'v2 모델 재학습 → AUC 0.85 달성', dept: '', blocked_by: [] },
    { task: 'x', dept: '', blocked_by: ['전혀 다른 것'] },
  ]);
  const w = renderDagWarnings(g);
  assert.match(w, /구버전 결과/);
  assert.match(w, /1개 중 0개만 연결/);
});

test('순환 경고는 화면에 노출된다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'a', dept: '', blocked_by: ['T2'] },
    { id: 'T2', task: 'b', dept: '', blocked_by: ['T1'] },
  ]);
  assert.match(renderDagWarnings(g), /순환 의존/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: FAIL — `Cannot find module '../B_직접/앱/dag-view.js'`

- [ ] **Step 3: `B_직접/앱/dag-view.js` 구현**

```js
/* 조정치 · DAG 뷰 — SVG 문자열만 만든다 (DOM 접근 없음, 순수).
   file:// 더블클릭 지원을 위해 클래식 스크립트. graph.js 다음에 로드되어야 한다. */
(function (root) {
  'use strict';

  var G = (typeof module !== 'undefined' && module.exports) ? require('./graph.js') : root.Graph;

  function esc(s) {
    return String(s == null ? '' : s).replace(/[&<>"']/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
    });
  }
  function clip(s, n) { s = String(s || ''); return s.length > n ? s.slice(0, n - 1) + '…' : s; }

  var DEPT_COLORS = ['#2563eb','#d97706','#0d9488','#7c3aed','#16a34a','#db2777','#0891b2','#ca8a04'];
  function deptColor(dept) {
    var s = 0, d = String(dept || '');
    for (var i = 0; i < d.length; i++) s = (s * 31 + d.charCodeAt(i)) >>> 0;
    return DEPT_COLORS[s % DEPT_COLORS.length];
  }

  var TOP = 28;

  function renderDag(g) {
  if (!g.nodes.length) return '';
  var W = G.NODE_W, H = G.NODE_H, GX = G.GAP_X, GY = G.GAP_Y;
  var maxD = 0, maxY = 0;
  g.nodes.forEach(function(n){ if(n.depth>maxD)maxD=n.depth; if(n.y>maxY)maxY=n.y; });
  var w = (maxD+1)*(W+GX)+40, h = maxY+H+56;
  var byId = {}; g.nodes.forEach(function(n){ byId[n.id]=n; });

  var out = '<svg viewBox="0 0 '+w+' '+h+'" class="dag" role="img" aria-label="크리티컬 패스 의존 그래프">';
  out += '<defs>'
      +  '<marker id="ah" markerWidth="9" markerHeight="9" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 z" fill="var(--line2)"/></marker>'
      +  '<marker id="ahc" markerWidth="9" markerHeight="9" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 z" fill="var(--chk)"/></marker>'
      +  '</defs>';

  // 열 라벨
  for (var d=0; d<=maxD; d++){
    var lx = 20 + d*(W+GX);
    out += '<text x="'+lx+'" y="16" class="dag-col">'+(d===0?'1단계 · 지금 시작 가능':(d+1)+'단계')+'</text>';
  }

  // 엣지를 노드보다 먼저 그려 뒤에 깔리게 한다
  g.edges.forEach(function(e){
    var a = byId[e.from], b = byId[e.to];
    var x1 = 20+a.x+W, y1 = TOP+a.y+H/2, x2 = 20+b.x, y2 = TOP+b.y+H/2;
    var mid = (x1+x2)/2;
    out += '<path d="M'+x1+','+y1+' C'+mid+','+y1+' '+mid+','+y2+' '+x2+','+y2+'"'
        +  ' class="'+(e.critical?'dag-e crit':'dag-e')+'" marker-end="url(#'+(e.critical?'ahc':'ah')+')"/>';
  });

  // 노드
  g.nodes.forEach(function(n){
    var x = 20+n.x, y = TOP+n.y;
    out += '<g class="dag-n'+(n.isBottleneck?' bott':'')+'">'
        +  '<rect x="'+x+'" y="'+y+'" width="'+W+'" height="'+H+'" rx="8"/>'
        +  '<rect x="'+x+'" y="'+y+'" width="4" height="'+H+'" rx="2" fill="'+deptColor(n.dept)+'"/>'
        +  '<text x="'+(x+14)+'" y="'+(y+18)+'" class="dag-t">'+esc(clip(n.task,22))+'</text>'
        +  '<text x="'+(x+14)+'" y="'+(y+32)+'" class="dag-d">'+esc(clip(n.dept,18))+'</text>';
    if (n.isBottleneck) out += '<text x="'+(x+14)+'" y="'+(y+44)+'" class="dag-b">⚠ 후행 '+n.downstream+'개가 이걸 기다림</text>';
    out += '</g>';
    // external 스텁
    n.externals.forEach(function(lb, k){
      var sx = x - 96, sy = y + k*16;
      out += '<g class="dag-x"><rect x="'+sx+'" y="'+sy+'" width="88" height="14" rx="4"/>'
          +  '<text x="'+(sx+5)+'" y="'+(sy+10)+'">? '+esc(clip(lb,9))+'</text></g>';
    });
  });
  return out + '</svg>';
  }

  function renderDagWarnings(g) {
    if (!g.warnings.length && g.stats.mode === 'id') return '';
    var h = '<div class="dag-warn">';
    if (g.stats.mode === 'fuzzy') {
      h += '<div class="w-row w-info">구버전 결과 · 의존관계 ' + g.stats.edgeTotal + '개 중 '
        + g.stats.edgeResolved + '개만 연결됨. 정확한 그래프를 보려면 다시 분석하세요.</div>';
    }
    g.warnings.forEach(function (w) {
      if (w.type === 'cycle')       h += '<div class="w-row w-bad">순환 의존: ' + esc(w.detail) + '</div>';
      else if (w.type === 'empty')  h += '<div class="w-row w-info">진행 순서를 뽑을 수 없었습니다.</div>';
      else if (w.type === 'single') h += '<div class="w-row w-info">작업이 하나뿐이라 의존 관계가 없습니다.</div>';
      else if (w.type === 'unresolved' && g.stats.mode === 'id') h += '<div class="w-row w-info">' + esc(w.detail) + '</div>';
    });
    return h + '</div>';
  }

  var api = { renderDag: renderDag, renderDagWarnings: renderDagWarnings, deptColor: deptColor };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else root.DagView = api;
})(typeof globalThis !== 'undefined' ? globalThis : this);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (25 tests)

- [ ] **Step 5: A에 복사 + 동일성 테스트 추가**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
cp B_직접/앱/dag-view.js A_웹페이지/앱/dag-view.js
```

`테스트/graph.test.js` 끝에 추가:

```js
test('A·B의 dag-view.js는 바이트 단위로 동일해야 한다', () => {
  const a = fs.readFileSync(path.join(__dirname, '..', 'A_웹페이지', '앱', 'dag-view.js'), 'utf8');
  const b = fs.readFileSync(path.join(__dirname, '..', 'B_직접', '앱', 'dag-view.js'), 'utf8');
  assert.equal(a, b, 'A와 B의 dag-view.js가 다릅니다 — 한쪽만 고쳤습니다');
});
```

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/`
Expected: PASS (26 tests)

- [ ] **Step 6: 두 index.html에 스크립트 태그 추가**

A·B 각각, Task 6에서 넣은 `<script src="graph.js"></script>` **바로 다음 줄**에 추가 (순서 중요 — `dag-view.js`가 `Graph`를 참조한다):

```html
<script src="dag-view.js"></script>
```

- [ ] **Step 7: 기존 `.rail` 렌더링을 교체**

A·B 각각의 `index.html:545-551` 에 있는 다음 블록 **전체**를 찾아:

```js
  if(d.sequence&&d.sequence.length){
    h+=secH('02','권장 진행 순서 · 크리티컬 패스');
    h+='<div class="rail"><ol class="track">'+d.sequence.map(s=>
      `<li class="node ${s.bottleneck?'bott':''}"><span class="dot"></span><div class="nc"><div class="tk">${esc(s.task)}${s.bottleneck?'<span class="flag">병목</span>':''}</div>`+
      ((s.dept||(s.blocked_by&&s.blocked_by.length))?`<div class="dep">${s.dept?esc(s.dept):''}${(s.blocked_by&&s.blocked_by.length)?` · 선행: <span class="dchip">${s.blocked_by.map(esc).join(', ')}</span>`:''}</div>`:'')+
      `</div></li>`).join('')+'</ol></div>';
  }
```

다음으로 교체:

```js
  const g = Graph.buildGraph(d.sequence||[]);
  h+=secH('02','권장 진행 순서 · 크리티컬 패스');
  h+=DagView.renderDagWarnings(g);
  if(g.nodes.length===1){
    h+=`<div class="dag-single">${esc(g.nodes[0].task)} · ${esc(g.nodes[0].dept)}</div>`;
  } else if(g.nodes.length){
    h+=`<div class="dag-wrap">${DagView.renderDag(g)}</div>`;
  }
```

> Task 8에서 이 블록을 히어로 안으로 옮긴다. 지금은 **자리만 그대로 두고 내용만 교체**한다 — 한 번에 하나씩.

기존 `.rail`, `.track`, `.node`, `.flag`, `.dchip` CSS 규칙은 이제 쓰이지 않는다. **삭제하지 말고 그대로 둔다** (Task 8에서 위계를 잡은 뒤 마지막에 정리한다).

- [ ] **Step 8: CSS 추가 (라이트/다크 both) — A·B 양쪽**

**새 CSS 변수를 추가하지 않는다.** 기존 토큰만 조합해서 라이트·다크 대응을 공짜로 얻는다. 스타일 시트에 다음을 추가:

❗ 이 프로젝트의 실제 CSS 변수만 쓴다. **`--accent`·`--text`·`--surface-2`·`--dag-edge` 는 존재하지 않는다.**
실재하는 것: `--chk --chk-bg --est --est-bg --faint --flow --ink --ink2 --inset --line --line2 --mono --muted --ok --ok-bg --paper --primary --primary-ink --primary-soft --radius --radius-sm --sans --shadow --shadow-sm --surface --surface2`

병목 표시에 `--chk`/`--chk-bg`(기존 '확인필요' 경고색)를 재사용한다 — **이미 라이트·다크 both 대응이라 별도 다크 오버라이드가 필요 없다.**

```css
/* 크리티컬 패스 · 계층형 DAG */
.dag-wrap{overflow-x:auto;background:var(--surface);border:1px solid var(--line);border-radius:var(--radius);padding:10px 14px 14px}
.dag{min-width:100%;height:auto;display:block}
.dag-col{font-size:10px;fill:var(--muted);letter-spacing:1px}
.dag-e{stroke:var(--line2);stroke-width:1.6;fill:none}
.dag-e.crit{stroke:var(--chk);stroke-width:2.6}
.dag-n rect:first-child{fill:var(--surface2);stroke:var(--line);stroke-width:1.2}
.dag-n.bott rect:first-child{fill:var(--chk-bg);stroke:var(--chk);stroke-width:2.6}
.dag-t{font-size:11.5px;font-weight:700;fill:var(--ink)}
.dag-d{font-size:10px;fill:var(--muted)}
.dag-b{font-size:9.5px;font-weight:700;fill:var(--chk)}
.dag-x rect{fill:none;stroke:var(--muted);stroke-width:1;stroke-dasharray:4 3;opacity:.7}
.dag-x text{font-size:8px;fill:var(--muted)}
.dag-single{padding:12px 14px;border:1px solid var(--line);border-radius:var(--radius);background:var(--surface)}
.dag-warn{margin-bottom:8px}
.w-row{font-size:12px;padding:6px 10px;border-radius:var(--radius-sm);margin-bottom:4px}
.w-info{background:var(--inset);color:var(--muted)}
.w-bad{background:var(--chk-bg);color:var(--chk);font-weight:700}
```

이 CSS 블록과 Step 7의 JS 교체는 **A·B 두 index.html에 동일하게** 넣는다. (JS 로직은 `dag-view.js`로 빠져 동일성 테스트가 지키지만, CSS와 이 호출부는 index.html 안에 있어 손으로 맞춰야 한다.)

- [ ] **Step 9: 실제 렌더링 확인 (증거 필수)**

```bash
cd "C:/Users/toyal/Desktop/새 폴더/러너/py"
python -u server.py ../../B_직접/앱 8787 &
```

브라우저로 `http://localhost:8787` → 기존 `결과/샘플01_결과.json` 을 [결과 붙여넣기]에 넣고 [보드 렌더링].

Expected:
- SVG 다이어그램이 그려진다 (텍스트 나열이 아니라)
- 상단에 `구버전 결과 · 의존관계 8개 중 N개만 연결됨` 고지가 뜬다
- 빨간 엣지가 병목에서만 나간다

`http://localhost:8788` (A 러너)에서도 동일하게 확인한다.

- [ ] **Step 10: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add B_직접/앱/dag-view.js A_웹페이지/앱/dag-view.js B_직접/앱/index.html A_웹페이지/앱/index.html 테스트/graph.test.js
git commit -m "feat(UI): 크리티컬 패스를 계층형 DAG 다이어그램으로 교체"
```

---

### Task 8: H1 위계 재배치

**Files:**
- Modify: `B_직접/앱/index.html`, `A_웹페이지/앱/index.html`

**목표 구조** (확정된 H1):

```
Tier 1 · 히어로   : 한 줄 요약(크게) + 지금 할 일 Top3 + 크리티컬 패스 DAG
Tier 2           : 부서별 실행항목 (6 카드, 중간 밀도)
Tier 3 · 기본 접힘: ▸ 의사결정 필요 (4) / ▸ 확인 필요·가정 (11) / ▸ 부서별 공유 메시지 (6)
```

**핵심 변경 3가지**:
1. 크리티컬 패스를 **2번째 섹션 → 히어로 안으로**
2. 공유 메시지·갭/가정을 **접는다** (복사할 때만 필요한 물건)
3. **의사결정을 부서 카드에서 빼내 최상위로 모은다** — 아래 설명

**의사결정 집계에 대하여**: 현재 의사결정은 독립 섹션이 아니라 **부서 카드 안에** `tblDec` 로 박혀 있다(`index.html:542`). H1은 이를 최상위 접힘 하나로 모은다. 근거: (1) 의사결정은 부서를 가로지른다 — 결정자가 다른 부서인 경우가 많아 부서별로 흩으면 "지금 뭘 결정해야 하나"가 안 보인다. (2) Tier 2의 밀도를 낮추는 것이 H1의 목적이다. **부서 카드에서는 제거**한다(중복 금지).

- [ ] **Step 1: 헬퍼 4개 추가**

`index.html:527` 의 `secH` 함수 **바로 다음**에 추가:

```js
/* Tier 3 — 기본 접힘. <details>는 JS 없이 브라우저가 열고 닫는다. */
function fold(title,count,inner){
  return `<details class="fold"><summary><span class="f-title">${esc(title)}</span><span class="f-count">${count}</span></summary><div class="f-body">${inner}</div></details>`;
}
/* 부서를 가로지르는 의사결정 통합 표 (부서 열이 추가된 tblDec) */
function tblDecAll(items){
  if(!items||!items.length)return '<div class="basis">의사결정이 필요한 항목이 없습니다.</div>';
  return `<table class="t"><tr><th>안건</th><th>부서</th><th>결정자</th><th>기한</th><th>선행조건</th></tr>`+
    items.map(d=>`<tr><td>${esc(d.topic)} ${tag(d.status)}</td><td>${esc(d._dept||'')}</td><td class="who">${esc(d.decider||'미정')}</td><td class="when">${esc(d.due||'미정')}</td><td>${esc(d.precondition||'')}</td></tr>`).join('')+`</table>`;
}
function gapsHtml(gaps,asms){
  return '<div class="cols2">'+
    `<div class="panelbox"><div class="block__l">❓ 확인 필요 (gaps)</div><div class="chips">${gaps.map(g=>`<span class="chip gap">${esc(g)}</span>`).join('')||'<span class="chip">없음</span>'}</div></div>`+
    `<div class="panelbox"><div class="block__l">🧩 가정 (assumptions)</div><div class="chips">${asms.map(a=>`<span class="chip asm">${esc(a)}</span>`).join('')||'<span class="chip">없음</span>'}</div></div>`+
    '</div>';
}
function shareHtml(sms){
  if(!sms.length)return '<div class="basis">공유 메시지가 없습니다.</div>';
  let s='';
  sms.forEach((m,i)=>{
    if(m.chat)s+=`<div class="msg"><div class="mh">💬 ${esc(m.dept)}<span class="kind">메신저</span><button class="btn mini cp" data-c="c${i}">복사</button></div><pre id="c${i}">${esc(m.chat)}</pre></div>`;
    if(m.email_body)s+=`<div class="msg"><div class="mh">✉️ ${esc(m.dept)}<span class="kind">이메일</span><button class="btn mini cp" data-c="e${i}">복사</button></div><pre id="e${i}">${esc((m.email_subject?('제목: '+m.email_subject+'\n\n'):'')+m.email_body)}</pre></div>`;
  });
  return s;
}
```

> 복사 버튼 배선(`index.html:571`)은 그대로 둔다. `<details>` 는 닫혀 있어도 자식이 DOM에 남으므로 `querySelectorAll('.msg .cp')` 가 계속 동작한다.

- [ ] **Step 2: `render()` 본문(`index.html:529-573`)을 재배치**

`render()` 의 `let h='';` 부터 `h+=\`<details class="raw">...\`` 직전까지를 다음으로 교체:

```js
  const g=Graph.buildGraph(d.sequence||[]);
  let h='';

  /* ── Tier 1 · 히어로: 요약 + 지금 할 일 Top3 + 크리티컬 패스 ── */
  h+=`<section class="summary hero"><div class="eyebrow">SUMMARY · ${esc(mt.title||'제목 미상')} · ${esc(mt.datetime||'일시 미상')}</div>`;
  h+=`<h2>${esc(d.headline||'회의 실행보드')}</h2>`;
  if(d.top_actions&&d.top_actions.length){h+='<div class="top3">'+d.top_actions.slice(0,3).map((t,i)=>
    `<div class="it"><span class="rk">${i+1}</span><div><div class="wt">${esc(t.what)}</div><div class="mt mono">${esc(t.dept||'')} · ${esc(t.owner||'미정')} · ${esc(t.due||'미정')}</div></div></div>`).join('')+'</div>';}
  h+=DagView.renderDagWarnings(g);
  if(g.nodes.length===1){
    h+=`<div class="dag-single">${esc(g.nodes[0].task)} · ${esc(g.nodes[0].dept)}</div>`;
  } else if(g.nodes.length){
    h+=`<div class="dag-wrap">${DagView.renderDag(g)}</div>`;
  }
  h+='</section>';

  /* ── Tier 2 · 부서별 실행 항목 (의사결정은 Tier 3으로 뺐다) ── */
  h+=secH('01','부서별 실행 항목');
  h+='<div class="depts">'+ (d.by_department||[]).map(dep=>
    `<section class="dept"><div class="dept__h"><span class="dot"></span><span class="nm">${esc(dep.dept)}</span></div><div class="dept__b">`+
    (tblAI(dep.action_items)+tblDoc(dep.documents) || '<div class="block"><div class="basis">추출된 항목 없음</div></div>')+
    `</div></section>`).join('') + '</div>';

  /* ── Tier 3 · 기본 접힘 ── */
  const decs=[];
  (d.by_department||[]).forEach(dep=>(dep.decisions_needed||[]).forEach(x=>decs.push(Object.assign({_dept:dep.dept},x))));
  h+=fold('의사결정 필요',decs.length,tblDecAll(decs));

  const gaps=(d.gaps||[]).filter(Boolean), asms=(d.assumptions||[]).filter(Boolean);
  h+=fold('확인 필요 · 가정',gaps.length+asms.length,gapsHtml(gaps,asms));

  h+=fold('부서별 공유 메시지',(d.share_messages||[]).length,shareHtml(d.share_messages||[]));
```

> `tblDec` 함수는 이제 `render()` 에서 호출되지 않지만 **삭제하지 않는다** — `tblDecAll` 이 같은 `tag()`·`.t` 스타일에 의존하고, 되돌리기 쉬워야 한다.

- [ ] **Step 3: 히어로 · 접힘 CSS 추가**

`.top3` 는 **이미 존재한다**(`index.html:219-225`, 620px 이상에서 3열). **재정의하지 말고 그대로 쓴다.**
히어로는 기존 `.summary` 를 강화하는 방식이다 — `class="summary hero"` 로 두 클래스를 함께 준다.

```css
/* Tier 1 — 히어로: 요약 + Top3 + 크리티컬 패스를 한 덩어리로 */
.summary.hero{border-width:2px;border-left-width:3px;border-color:var(--primary);box-shadow:var(--shadow);margin-bottom:22px}
.summary.hero h2{font-size:22px;margin:8px 0 16px}
.summary.hero .dag-wrap{margin-top:16px;background:var(--surface2)}
@media(max-width:620px){.summary.hero h2{font-size:18px}}

/* Tier 3 — 기본 접힘 */
.fold{border:1px solid var(--line);border-radius:var(--radius);background:var(--surface);margin-bottom:8px;box-shadow:var(--shadow-sm)}
.fold summary{cursor:pointer;padding:12px 16px;display:flex;align-items:center;gap:8px;font-size:13.5px;font-weight:700;list-style:none;color:var(--ink)}
.fold summary::-webkit-details-marker{display:none}
.fold summary::before{content:'▸';color:var(--muted);transition:transform .15s;display:inline-block}
.fold[open] summary::before{transform:rotate(90deg)}
.f-title{flex:1}
.f-count{background:var(--inset);border:1px solid var(--line);border-radius:10px;padding:1px 9px;font-size:11px;font-family:var(--mono);color:var(--muted);font-weight:700}
.f-body{padding:0 16px 16px}
```

- [ ] **Step 4: A에도 동일 적용**

`A_웹페이지/앱/index.html` 에 Step 1~3의 코드를 **그대로** 넣는다. (A·B의 `render()` 로직은 동일해야 한다. 다른 것은 임베드된 시스템 프롬프트와 `<title>` 뿐이다.)

- [ ] **Step 5: 렌더링 확인 (증거 필수)**

`http://localhost:8787` 에서 샘플01 결과를 렌더링.

Expected:
- 히어로가 시각적으로 압도적 — 요약 + Top3 + DAG가 한 덩어리
- 부서별 카드는 중간 밀도 — **의사결정 블록이 카드 안에 없다**(Tier 3으로 이동했으므로)
- 의사결정 · 갭/가정 · 공유메시지는 **접힌 상태**로 개수 배지만 보임
- 의사결정 접힘을 펼치면 **부서 열이 있는 통합 표**가 나온다
- 공유 메시지 접힘을 펼치고 [복사] 버튼이 여전히 동작한다
- 라이트/다크 both 확인 (`--chk` 계열을 재사용했으므로 다크에서도 병목이 읽혀야 한다)

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add B_직접/앱/index.html A_웹페이지/앱/index.html
git commit -m "feat(UI): H1 3단 위계 — 히어로(요약+Top3+DAG) / 부서별 / 접힘 3종"
```

---

### Task 9: 프롬프트 스키마 개편 (A·B)

**Files:**
- Modify: `B_직접/앱/AGENT.md:78` 부근 (JSON 스키마), `B_직접/시스템프롬프트.md`
- Modify: `A_웹페이지/앱/AGENT.md`, `A_웹페이지/시스템프롬프트.md`

**공정성 제약 (재확인)**: A는 **JSON 출력 계약 블록만** 고친다. A의 분석 규칙(본부 나열 방식, '확인 필요' 단일 등급, 날짜 표현 등)은 **한 글자도 건드리지 않는다.** B는 자유롭게 고쳐도 된다.

- [ ] **Step 1: B의 sequence 스키마 교체**

`B_직접/앱/AGENT.md:78` 의 다음 줄을 찾아:

```
  "sequence": [{"step":1,"task":"","dept":"","blocked_by":[],"blocks":[],"bottleneck":false}],
```

교체:

```
  "sequence": [{"id":"T1","task":"","dept":"","blocked_by":[],"blocked_by_external":[]}],
```

- [ ] **Step 2: B의 STEP 4 지침 교체**

`B_직접/앱/AGENT.md:46-47` 의 다음을 찾아:

```
STEP 4. 전체 선후행을 위상정렬해 "권장 진행 순서(sequence)"를 1,2,3… 으로 제시.
        각 단계에 blocked_by(선행)·blocks(후행)를 표기. 병목·순환의존은 경고로 표시.
```

교체:

```
STEP 4. 후속 작업을 sequence 배열로 낸다. 각 항목에 id("T1","T2"… 배열 순서대로)를 붙인다.
        - blocked_by 에는 "이 작업보다 반드시 먼저 끝나야 하는 작업"의 id만 적는다.
        - 선행이 sequence 안의 작업이 아니면(회의록에 언급만 되고 작업으로 안 잡힌 것)
          blocked_by_external 에 그 이름을 문자열로 적는다. 없는 작업을 지어내지 마라.
        - 후행(blocks)·병목·진행 순서 번호는 적지 마라. 화면이 blocked_by 로부터 계산한다.
          같은 사실을 두 군데 적으면 어긋난다.
```

- [ ] **Step 3: B에 자기검증 스텝 추가**

`B_직접/앱/AGENT.md` 의 마지막 STEP(자기검증) 항목에 다음을 추가:

```
- sequence 의 모든 blocked_by 항목이 sequence 안에 실제로 존재하는 id인지 하나씩 확인하라.
  존재하지 않으면 그 항목을 blocked_by_external 로 옮겨라. 없는 id를 남기지 마라.
- 스스로를 blocked_by 에 넣지 마라.
```

- [ ] **Step 4: `B_직접/시스템프롬프트.md` 에 동일 반영**

`B_직접/시스템프롬프트.md` 는 `AGENT.md` 와 같은 프롬프트의 문서용 사본이다. 위 3개 변경을 그대로 반영해 두 파일이 어긋나지 않게 한다.

- [ ] **Step 5: A의 JSON 스키마 블록만 교체**

`A_웹페이지/앱/AGENT.md` 와 `A_웹페이지/시스템프롬프트.md` 에서 `sequence` JSON 스키마 줄을 찾아 Step 1과 동일한 형태로 교체하고, Step 2·3의 지침을 **출력 계약 설명으로서만** 추가한다.

❗ A의 다음 항목은 **절대 수정 금지** (비교 대상이다):
- 본부/부서를 나열하는 방식
- '확인 필요' 단일 등급 (B의 3단계로 바꾸지 말 것)
- 날짜 표현 규칙 (상대날짜 유지 — B의 절대날짜 환산을 이식하지 말 것)
- Top3·요약 유무

- [ ] **Step 6: 스키마 문자열이 양쪽에 반영됐는지 확인**

Run:
```bash
cd "C:/Users/toyal/Desktop/새 폴더"
grep -c "blocked_by_external" A_웹페이지/앱/AGENT.md A_웹페이지/시스템프롬프트.md B_직접/앱/AGENT.md B_직접/시스템프롬프트.md
grep -c "\"blocks\"\|bottleneck" A_웹페이지/앱/AGENT.md B_직접/앱/AGENT.md
```
Expected: 첫 명령 — 4개 파일 모두 1 이상. 둘째 명령 — 모두 **0**.

- [ ] **Step 7: 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add A_웹페이지/앱/AGENT.md A_웹페이지/시스템프롬프트.md B_직접/앱/AGENT.md B_직접/시스템프롬프트.md
git commit -m "feat(프롬프트): sequence 스키마를 id 참조로 — blocks/bottleneck/step 제거, external 신설"
```

---

### Task 10: 신스키마 결과 재생성 + end-to-end 검증 + 문서 마무리

**Files:**
- Regenerate: `B_직접/앱/결과/`, `A_웹페이지/앱/결과/`
- Modify: `B_직접/실행결과_샘플01.md`, `A_웹페이지/실행결과_샘플01.md`
- Modify: `문서/평가_비교.md`, `README.md`
- Replace: `문서/스크린샷/실행보드_라이트.png`, `문서/스크린샷/실행보드_다크.png`

**증거 없는 완료 선언 금지.** 이 프로젝트에는 "검증 완료"라고 적혔지만 사실이 아니었던 항목이 2건 있었다(설계 1-3, 1-5).

- [ ] **Step 1: 전체 단위 테스트 통과 확인**

Run: `cd "C:/Users/toyal/Desktop/새 폴더" && node --test 테스트/ && python -m unittest 테스트.stream_parse_test`
Expected: 전부 PASS. **인수 테스트(샘플01 엣지 8/8 · 병목 v2 · 후행 4) 포함.**

- [ ] **Step 2: B 러너로 샘플01 신스키마 결과 생성**

```bash
cd "C:/Users/toyal/Desktop/새 폴더/러너/py"
python -u server.py ../../B_직접/앱 8787 &
```
브라우저 `http://localhost:8787` → 회의록 `샘플01_불규칙메모.md` 선택 → [회의록 분석].

Expected: 생성 과정이 **실시간으로 스트리밍**된다(Task 1 검증). 완료 후 `B_직접/앱/결과/` 에 새 JSON 저장.

- [ ] **Step 3: 생성된 결과가 신스키마인지 + 엔진이 완전 연결하는지 확인**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
node -e "
const fs=require('fs'),path=require('path');
const dir='B_직접/앱/결과';
const f=fs.readdirSync(dir).filter(x=>x.endsWith('.json')).map(x=>({x,t:fs.statSync(path.join(dir,x)).mtimeMs})).sort((a,b)=>b.t-a.t)[0].x;
const d=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));
const {buildGraph}=require('./B_직접/앱/graph.js');
const g=buildGraph(d.sequence);
console.log('파일:',f);
console.log('mode:',g.stats.mode);
console.log('연결:',g.stats.edgeResolved+'/'+g.stats.edgeTotal);
console.log('경고:',g.warnings.map(w=>w.type));
console.log('병목:',g.nodes.filter(n=>n.isBottleneck).map(n=>n.task+' (후행 '+n.downstream+')'));
console.log('구스키마 잔재:', ['blocks','bottleneck','step'].filter(k=>d.sequence.some(s=>k in s)));
"
```

Expected:
- `mode: id`
- `연결: N/N` (**edgeResolved === edgeTotal**)
- `경고:` 에 `unresolved`·`cycle` 없음
- `구스키마 잔재: []`

❗ `mode: fuzzy` 가 나오면 프롬프트가 id를 안 낸 것이다 → Task 9로 돌아간다.

- [ ] **Step 4: A 러너로 동일 반복**

```bash
cd "C:/Users/toyal/Desktop/새 폴더/러너/py"
python -u server.py ../../A_웹페이지/앱 8788 &
```
`http://localhost:8788` 에서 샘플01 분석 → Step 3의 명령을 `A_웹페이지/앱/결과` 로 바꿔 동일 확인.

- [ ] **Step 5: 샘플02~04(정상·애매·예외)로 견고성 확인**

B 앱에서 `샘플02_정상케이스`, `샘플03_애매케이스`, `샘플04_예외케이스` 를 각각 분석.

Expected: 크래시 없음. 샘플03·04처럼 정보가 부족한 입력에서는 **작업을 지어내지 않고** `blocked_by_external` 또는 빈 sequence + 경고로 처리된다.

- [ ] **Step 6: 스크린샷 재촬영 (라이트·다크)**

샘플01 신스키마 결과가 렌더링된 상태에서 전체 페이지를 촬영해 교체:
- `문서/스크린샷/실행보드_라이트.png`
- `문서/스크린샷/실행보드_다크.png`

Expected: 히어로(요약+Top3+DAG)가 상단을 지배하고, 하단 3개 섹션은 접힌 상태로 보인다.

- [ ] **Step 7: `실행결과_샘플01.md` 갱신**

A·B 각각의 `실행결과_샘플01.md` 를 새 결과로 갱신한다. 신스키마 JSON과 실제 보드 내용이 일치해야 한다.

- [ ] **Step 8: `문서/평가_비교.md` 에 공정성 선긋기 명시**

5절 뒤에 다음 절을 추가:

```markdown
## 5-1. 개편 이후의 공정성 (2026-07-15)

A·B 비교 실험 종료 후, 두 앱에 동일한 UI 개편(3단 위계 + 계층형 DAG)과 동일한 JSON 출력 계약
(`sequence` id 참조 · `blocked_by_external` · `blocks`/`bottleneck`/`step` 제거)을 적용했다.

- **양쪽 동일 적용**: JSON 출력 계약, 그래프 엔진(`graph.js` — 바이트 동일), 렌더러, 위계
- **손대지 않음(= 비교 대상)**: A의 본부 나열 vs B의 매핑 규칙표 / A의 '확인 필요' 단일 등급 vs
  B의 확정·추정·확인필요 3단계 / B의 상대→절대 날짜 환산 / B의 Top3·자기검증

근거: JSON 출력 계약은 *앱이 데이터를 받아먹는 인터페이스*이지 *분석 능력*이 아니다.
앞서 "A의 앱도 같은 코크핏 형태로 제작"한 것과 같은 성격의 조치다.
따라서 2절 점수표의 **커버리지·정확도·견고함** 항목은 여전히 유효하다.

**병목은 이제 모델의 의견이 아니라 계산값이다.** 개편 전 `bottleneck: true` 는 모델이 직접 찍은 값이었고,
개편 후에는 A·B 모두 동일한 엔진이 후행 개수로 계산한다. 이 항목에서는 A·B 차이가 사라졌다.
```

- [ ] **Step 9: `README.md` 갱신**

- "UI 요약" 문단을 H1 3단 위계 + 계층형 DAG 기준으로 갱신
- "디자인 & 검증" 문단의 `시그니처 크리티컬 패스 레일(진행순서·병목 시각화)` → `계층형 DAG(의존 그래프·병목 자동 계산)` 로 교체
- 폴더 지도에 `테스트/`, `docs/superpowers/`, `앱/graph.js` 추가
- 검증 문구는 **실제로 통과한 것만** 적는다

- [ ] **Step 10: 최종 커밋**

```bash
cd "C:/Users/toyal/Desktop/새 폴더"
git add -A
git commit -m "feat: 신스키마 결과 재생성 + 문서/스크린샷 갱신 + 공정성 선긋기 명시"
```

---

## 완료 기준 (전부 증거 필요)

- [ ] `node --test 테스트/` 전부 통과 — **인수 테스트 포함** (샘플01 신스키마: 엣지 8/8 · 병목 = v2 재학습 · 후행 4)
- [ ] `python -m unittest 테스트.stream_parse_test` 통과
- [ ] A·B `graph.js` · `dag-view.js` 바이트 동일 (테스트로 강제)
- [ ] `file://` 더블클릭에서 `Graph.buildGraph` 동작 (Task 6 Step 6)
- [ ] 회의록 문자열이 SVG로 이스케이프됨 (XSS 테스트)
- [ ] 부서 카드에 의사결정 블록이 남아있지 않음 (Tier 3으로 이동 완료, 중복 없음)
- [ ] 자동 모드에서 **완료 전에** 텍스트 조각이 스트리밍됨 (Task 1 Step 8)
- [ ] A·B 신스키마 재생성 결과가 `mode:'id'` · `edgeResolved === edgeTotal`
- [ ] 구버전 결과 파일도 크래시 없이 렌더링 + `N/8 연결됨` 고지 노출
- [ ] 라이트·다크 both 스크린샷 갱신
- [ ] `README.md` · `문서/평가_비교.md` 에 사실과 다른 서술이 남아있지 않음
