# 실행보드 v2 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 크리티컬 패스 DAG 노드에 클릭 상세(사이드 패널)를 붙이고, 확인필요·가정 섹션을 그래프 연결도 기준 top5+더보기로 압축하며, 부서 카드 높이/여백을 정리한다.

**Architecture:** 순수 파생 로직은 신규 모듈 `board-derive.js`(그래프+보드데이터→뷰모델)에 모으고 fixture로 TDD. `graph.js`는 매칭 함수만 노출, `dag-view.js`는 노드에 클릭 타깃 속성과 패널 본문 생성기를 추가. `index.html`은 패널 DOM/CSS/배선과 랭킹 렌더만 담당한다. 프롬프트·스키마·러너는 건드리지 않는다.

**Tech Stack:** 클래식 스크립트(IIFE, ES모듈 아님 — `file://` 더블클릭 지원), `node:test`+`node:assert`, Playwright(시각 검증).

**Spec:** `docs/superpowers/specs/2026-07-19-실행보드-v2개선-design.md`

## Global Constraints

- **A·B 바이트 동일**: `graph.js`·`dag-view.js`·`board-derive.js`는 A·B가 바이트 단위로 같아야 한다. 작업 순서: **B_직접에서 편집 → `cp`로 A_웹페이지에 복사 → 동일성 테스트**. 테스트는 항상 `../B_직접/앱/...`에서 require한다.
- **프롬프트/JSON 스키마/러너/시스템프롬프트 변경 금지** (범위 밖).
- **오프라인 `file://` 지원**: 새 CDN 의존 추가 금지. 모든 신규 스크립트는 IIFE 클래식 스크립트.
- **라이트/다크 둘 다** 스타일. `prefers-reduced-motion`은 전역 `*{animation:none;transition:none}`로 이미 처리됨 — 새 트랜지션도 자동 적용됨.
- **결정적**: 순수 모듈은 `Date.now()`/`Math.random()` 금지.
- **조인 임계값 0.5(하드) / 랭킹 임계값 0.35(소프트)** — 실측 근거는 스펙 §4-1.
- **테스트 실행**: `node --test 테스트/graph.test.js` (종료코드 0 = 통과). 현재 26개 전부 green이 baseline.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `{A,B}/앱/graph.js` | 순수 그래프 엔진 + `normalize`/`containment` 노출 | 수정(추가 노출만) |
| `{A,B}/앱/board-derive.js` | 그래프+보드데이터 → 조인/랭킹 (순수) | **신규** |
| `{A,B}/앱/dag-view.js` | 그래프 → SVG 문자열 + 노드 클릭 속성 + 패널 본문 | 수정 |
| `{A,B}/앱/index.html` | 패널 DOM/CSS/배선 · 랭킹 렌더 · 카드 CSS · **위계 히어로+아코디언** | 수정 |
| `테스트/graph.test.js` | 엔진·뷰·파생 단위 테스트 + A·B 동일성 | 수정(추가) |
| `테스트/fixtures/샘플01_풀보드.json` | Playwright 시각검증용 풀보드 데이터 | **신규** |

---

## Task 1: graph.js — `normalize`·`containment` 노출

**Files:**
- Modify: `B_직접/앱/graph.js` (api 객체) → `cp` → `A_웹페이지/앱/graph.js`
- Test: `테스트/graph.test.js`

**Interfaces:**
- Produces: `Graph.normalize(s) -> string`, `Graph.containment(a, b) -> number(0..1)` (기존 내부 함수를 그대로 노출). board-derive.js가 재사용한다.

- [ ] **Step 1: 실패 테스트 작성** — `테스트/graph.test.js` 맨 끝에 추가

```js
const { normalize, containment } = require('../B_직접/앱/graph.js');

test('graph.js가 normalize·containment를 노출한다 (board-derive 재사용용)', () => {
  assert.equal(typeof normalize, 'function');
  assert.equal(typeof containment, 'function');
  // 괄호·특수문자 제거 후 짧은 정식명칭이 긴 노드명에 포함 → 높은 포함계수
  assert.ok(
    containment('피처 추가 후 v2 모델 재학습 (AUC 0.85 목표)', 'v2 모델 재학습 → AUC 0.85 달성') >= 0.5,
    '실측 0.85 근처여야 한다'
  );
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test 테스트/graph.test.js`
Expected: FAIL — `typeof normalize` 가 `'undefined'` (아직 노출 안 됨)

- [ ] **Step 3: graph.js api에 노출 추가** — `B_직접/앱/graph.js`에서 아래를 찾아

```js
  var api = {
    buildGraph: buildGraph,
    NODE_W: NODE_W, NODE_H: NODE_H, GAP_X: GAP_X, GAP_Y: GAP_Y
  };
```

다음으로 교체:

```js
  var api = {
    buildGraph: buildGraph,
    normalize: normalize, containment: containment,
    NODE_W: NODE_W, NODE_H: NODE_H, GAP_X: GAP_X, GAP_Y: GAP_Y
  };
```

- [ ] **Step 4: A로 복사(바이트 동일)**

Run: `cp "B_직접/앱/graph.js" "A_웹페이지/앱/graph.js"`

- [ ] **Step 5: 통과 확인** (신규 테스트 + 기존 graph.js 동일성 테스트)

Run: `node --test 테스트/graph.test.js`
Expected: PASS (종료코드 0). 기존 26 + 신규 1 모두 green.

- [ ] **Step 6: 커밋**

```bash
git add "테스트/graph.test.js" "A_웹페이지/앱/graph.js" "B_직접/앱/graph.js"
git commit -m "feat(엔진): normalize·containment 노출 — board-derive 재사용용"
```

---

## Task 2: board-derive.js — `buildDetailMap` (#2 조인)

**Files:**
- Create: `B_직접/앱/board-derive.js` → `cp` → `A_웹페이지/앱/board-derive.js`
- Test: `테스트/graph.test.js`

**Interfaces:**
- Consumes: `Graph.containment` (Task 1), `Graph` 전역/모듈.
- Produces: `BoardDerive.buildDetailMap(graph, byDepartment) -> { [nodeId]: {owner, due, status, basis, matched:boolean} }`. `byDepartment`는 index.html의 `normalize()`가 만든 `[{dept, action_items:[{what,owner,due,status,basis}]}]` 형태.

- [ ] **Step 1: 실패 테스트 작성** — `테스트/graph.test.js` 끝에 추가

```js
const { buildDetailMap } = require('../B_직접/앱/board-derive.js');

test('buildDetailMap: 노드 task를 action_item(what)에 조인해 담당/기한/상태를 붙인다', () => {
  const d = JSON.parse(fs.readFileSync(path.join(__dirname, 'fixtures', '샘플01_신스키마.json'), 'utf8'));
  const g = buildGraph(d.sequence);
  const byDept = [
    { dept: 'CB본부', action_items: [
      { what: '피처 추가 후 v2 모델 재학습 (AUC 0.85 목표, 현재 0.82)', owner: '박리드', due: '확인 필요', status: '확인필요', basis: '' },
    ] },
  ];
  const map = buildDetailMap(g, byDept);
  assert.equal(map['T2'].matched, true, 'v2 재학습 노드가 조인되어야 한다');
  assert.equal(map['T2'].owner, '박리드');
  assert.equal(map['T2'].status, '확인필요');
  assert.equal(map['T9'].matched, false, '대응 action_item이 없는 노드는 matched:false');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test 테스트/graph.test.js`
Expected: FAIL — `Cannot find module '../B_직접/앱/board-derive.js'`

- [ ] **Step 3: board-derive.js 생성** — `B_직접/앱/board-derive.js` (아래 전체)

```js
/* 조정치 · 보드 파생 — 그래프 + 보드데이터 → 뷰모델. 순수(DOM 접근 없음).
   graph.js 다음에 로드되어야 한다. file:// 지원을 위해 클래식 스크립트. A·B 바이트 동일. */
(function (root) {
  'use strict';

  var G = (typeof module !== 'undefined' && module.exports) ? require('./graph.js') : root.Graph;

  var JOIN_THRESH = 0.5;   // 조인은 특정 담당/기한을 드러내므로 하드
  var RANK_THRESH = 0.35;  // 랭킹은 소프트 신호 — gap 노이즈 어미로 희석되어 느슨(스펙 §4-1)

  function bestMatch(query, candidates, getText, thresh) {
    var best = null, bestScore = 0;
    for (var i = 0; i < candidates.length; i++) {
      var s = G.containment(query, getText(candidates[i]));
      if (s > bestScore) { bestScore = s; best = candidates[i]; }
    }
    return bestScore >= thresh ? best : null;
  }

  function itemText(a) { return a.what || ''; }
  function nodeText(n) { return n.task || ''; }

  // 노드 → {owner,due,status,basis,matched}. action_items가 유일 출처, 여기선 조인만 한다.
  function buildDetailMap(graph, byDepartment) {
    var depts = byDepartment || [];
    var byDept = {}, all = [];
    depts.forEach(function (d) {
      var items = d.action_items || [];
      byDept[d.dept] = items;
      items.forEach(function (a) { all.push(a); });
    });
    var map = {};
    (graph.nodes || []).forEach(function (n) {
      var m = bestMatch(n.task, byDept[n.dept] || [], itemText, JOIN_THRESH);
      if (!m) m = bestMatch(n.task, all, itemText, JOIN_THRESH);
      map[n.id] = m
        ? { owner: m.owner || '', due: m.due || '', status: m.status || '', basis: m.basis || '', matched: true }
        : { owner: '', due: '', status: '', basis: '', matched: false };
    });
    return map;
  }

  // id에서 도달 가능한 후행 id 집합
  function reachable(graph, id) {
    var succ = {};
    (graph.nodes || []).forEach(function (n) { succ[n.id] = []; });
    (graph.edges || []).forEach(function (e) { if (succ[e.from]) succ[e.from].push(e.to); });
    var seen = {}, stack = [id];
    while (stack.length) {
      var cur = stack.pop();
      (succ[cur] || []).forEach(function (t) { if (!seen[t]) { seen[t] = true; stack.push(t); } });
    }
    return Object.keys(seen);
  }

  // 확인필요/가정 문자열을 그래프 연결도로 우선순위화(내림차순 정렬해 반환).
  function rankItems(graph, items) {
    var arr = (items || []).filter(Boolean);
    var byId = {}; (graph.nodes || []).forEach(function (n) { byId[n.id] = n; });
    var scored = arr.map(function (text, i) {
      var m = bestMatch(text, graph.nodes || [], nodeText, RANK_THRESH);
      var cpScore = 0, deptSpan = 0, downstream = 0;
      if (m) {
        downstream = m.downstream || 0;
        var seen = {};
        reachable(graph, m.id).forEach(function (rid) {
          var dp = byId[rid] && byId[rid].dept; if (dp) seen[dp] = true;
        });
        deptSpan = Object.keys(seen).length;
        cpScore = m.isBottleneck ? 2 : (downstream > 0 ? 1 : 0);
      } else {
        var hitExt = (graph.nodes || []).some(function (n) {
          return (n.externals || []).some(function (lb) { return G.containment(text, lb) >= RANK_THRESH; });
        });
        if (hitExt) cpScore = 1;
      }
      return { text: text, cpScore: cpScore, deptSpan: deptSpan, downstream: downstream, _i: i };
    });
    scored.sort(function (a, b) {
      return (b.cpScore - a.cpScore) || (b.deptSpan - a.deptSpan) || (b.downstream - a.downstream) || (a._i - b._i);
    });
    return scored;
  }

  var api = { buildDetailMap: buildDetailMap, rankItems: rankItems };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else root.BoardDerive = api;
})(typeof globalThis !== 'undefined' ? globalThis : this);
```

> `rankItems`는 Task 3에서 테스트하지만, 파일을 두 번 만들지 않도록 여기서 함께 작성한다.

- [ ] **Step 4: A로 복사**

Run: `cp "B_직접/앱/board-derive.js" "A_웹페이지/앱/board-derive.js"`

- [ ] **Step 5: 통과 확인**

Run: `node --test 테스트/graph.test.js`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add "테스트/graph.test.js" "A_웹페이지/앱/board-derive.js" "B_직접/앱/board-derive.js"
git commit -m "feat(파생): board-derive.buildDetailMap — 노드↔action_item 조인(#2)"
```

---

## Task 3: board-derive.js — `rankItems` 인수 테스트 (#3 랭킹) + A·B 동일성

**Files:**
- Test: `테스트/graph.test.js` (구현은 Task 2에서 완료됨 — 여기선 랭킹 인수 기준과 동일성만)

**Interfaces:**
- Consumes: `BoardDerive.rankItems(graph, items) -> [{text, cpScore, deptSpan, downstream, _i}]` 내림차순.

- [ ] **Step 1: 실패 테스트 작성** — `테스트/graph.test.js` 끝에 추가

```js
const { rankItems } = require('../B_직접/앱/board-derive.js');

test('[인수] rankItems: 병목에 걸린 gap이 1위, 무관한 별건 gap이 꼴찌', () => {
  const d = JSON.parse(fs.readFileSync(path.join(__dirname, 'fixtures', '샘플01_신스키마.json'), 'utf8'));
  const g = buildGraph(d.sequence); // 병목 = T2(v2 재학습), downstream 4
  const gaps = [
    '데사팀 채용 1명은 별건으로 보류',      // 미매칭 → cpScore 0
    'v2 모델 재학습 완료 기한 미기재',        // T2 병목 → cpScore 2 (실측 0.46, RANK_THRESH 0.35로 매칭)
    'API 응답속도 최적화 기한 미기재',        // T5 → cpScore 1
  ];
  const ranked = rankItems(g, gaps).map(r => r.text);
  assert.equal(ranked[0], 'v2 모델 재학습 완료 기한 미기재', '병목 gap이 최상위');
  assert.equal(ranked[ranked.length - 1], '데사팀 채용 1명은 별건으로 보류', '미매칭 별건이 꼴찌');
});

test('rankItems: 외부 스텁(제품 스펙 확정)을 가리키는 gap은 cpScore 1', () => {
  const d = JSON.parse(fs.readFileSync(path.join(__dirname, 'fixtures', '샘플01_신스키마.json'), 'utf8'));
  const g = buildGraph(d.sequence);
  const ranked = rankItems(g, ['제품 스펙 확정 시점 미정', '데사팀 별건 보류']);
  const ext = ranked.find(r => r.text.indexOf('제품 스펙') === 0);
  assert.equal(ext.cpScore, 1, '외부 선행 매칭 → 준-critical');
});

test('rankItems: 빈 입력은 빈 배열', () => {
  const g = buildGraph([{ id: 'T1', task: 'a', dept: '', blocked_by: [] }]);
  assert.deepEqual(rankItems(g, []), []);
  assert.deepEqual(rankItems(g, null), []);
});

test('A·B의 board-derive.js는 바이트 단위로 동일해야 한다', () => {
  const a = fs.readFileSync(path.join(__dirname, '..', 'A_웹페이지', '앱', 'board-derive.js'), 'utf8');
  const b = fs.readFileSync(path.join(__dirname, '..', 'B_직접', '앱', 'board-derive.js'), 'utf8');
  assert.equal(a, b, 'A와 B의 board-derive.js가 다릅니다 — 한쪽만 고쳤습니다');
});
```

- [ ] **Step 2: 실행 — 통과해야 함** (구현은 Task 2에 이미 있음)

Run: `node --test 테스트/graph.test.js`
Expected: PASS. 만약 `ranked[0]`이 다르면 `RANK_THRESH`(0.35)와 정렬키를 board-derive.js에서 재확인.

- [ ] **Step 3: 커밋**

```bash
git add "테스트/graph.test.js"
git commit -m "test(파생): rankItems 인수 기준(병목 gap 최상위) + A·B 동일성"
```

---

## Task 4: dag-view.js — 노드 `data-id`·a11y 속성

**Files:**
- Modify: `B_직접/앱/dag-view.js` (`renderDag`의 노드 `<g>`) → `cp` → A
- Test: `테스트/graph.test.js`

**Interfaces:**
- Produces: `renderDag(g)` 출력의 각 노드 `<g>`에 `data-id="<id>"`, `role="button"`, `tabindex="0"`, `aria-label="<task> · <dept>"`.

- [ ] **Step 1: 실패 테스트 작성** — `테스트/graph.test.js` 끝에 추가

```js
test('renderDag: 각 노드 <g>에 data-id·role·tabindex가 붙는다 (클릭·키보드 타깃)', () => {
  const g = buildGraph([
    { id: 'T1', task: '재학습', dept: 'CB본부', blocked_by: [] },
    { id: 'T2', task: 'API', dept: 'ICT본부', blocked_by: ['T1'] },
  ]);
  const svg = renderDag(g);
  assert.match(svg, /data-id="T1"/);
  assert.match(svg, /data-id="T2"/);
  assert.match(svg, /role="button"/);
  assert.match(svg, /tabindex="0"/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test 테스트/graph.test.js`
Expected: FAIL — `data-id="T1"` 없음

- [ ] **Step 3: 노드 `<g>` 수정** — `B_직접/앱/dag-view.js`에서 아래를 찾아

```js
    out += '<g class="dag-n'+(n.isBottleneck?' bott':'')+'">'
```

다음으로 교체:

```js
    out += '<g class="dag-n'+(n.isBottleneck?' bott':'')+'" data-id="'+esc(n.id)+'" role="button" tabindex="0" aria-label="'+esc(n.task+' · '+n.dept)+'">'
```

- [ ] **Step 4: A로 복사**

Run: `cp "B_직접/앱/dag-view.js" "A_웹페이지/앱/dag-view.js"`

- [ ] **Step 5: 통과 확인** (신규 + 기존 DAG 테스트 + dag-view 동일성)

Run: `node --test 테스트/graph.test.js`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add "테스트/graph.test.js" "A_웹페이지/앱/dag-view.js" "B_직접/앱/dag-view.js"
git commit -m "feat(뷰): DAG 노드에 data-id·a11y 속성 — 클릭/키보드 타깃(#2)"
```

---

## Task 5: dag-view.js — `nodePanelHtml`

**Files:**
- Modify: `B_직접/앱/dag-view.js` (`statusClass` + `nodePanelHtml` + api) → `cp` → A
- Test: `테스트/graph.test.js`

**Interfaces:**
- Consumes: `deptColor`(기존, 같은 파일).
- Produces: `DagView.nodePanelHtml(node, detail, graph) -> string`. `detail`은 `buildDetailMap`의 항목(`{matched, owner, due, status, basis}`).

- [ ] **Step 1: 실패 테스트 작성** — `테스트/graph.test.js` 끝에 추가

```js
const { nodePanelHtml } = require('../B_직접/앱/dag-view.js');

test('nodePanelHtml: 매칭 상세는 담당/기한/상태를 담고, 병목은 후행 수를 담는다', () => {
  const g = buildGraph([
    { id: 'T1', task: 'v2 재학습', dept: 'CB본부', blocked_by: [] },
    { id: 'T2', task: 'API', dept: 'ICT본부', blocked_by: ['T1'] },
  ]);
  const n = g.nodes.find(x => x.id === 'T1'); // 병목, 후행 1
  const html = nodePanelHtml(n, { matched: true, owner: '박리드', due: '7/15', status: '확인필요', basis: '' }, g);
  assert.match(html, /박리드/);
  assert.match(html, /7\/15/);
  assert.match(html, /확인필요/);
  assert.match(html, /후행 1개/);
});

test('nodePanelHtml: 미매칭이면 부서 표 참조 안내로 축약한다', () => {
  const g = buildGraph([{ id: 'T1', task: 'a', dept: 'X', blocked_by: [] }]);
  const html = nodePanelHtml(g.nodes[0], { matched: false }, g);
  assert.match(html, /부서별 실행 항목/);
});

test('nodePanelHtml: 외부 선행을 나열하고 회의록 문자열을 이스케이프한다', () => {
  const g = buildGraph([
    { id: 'T1', task: '<b>x</b>', dept: '', blocked_by: [], blocked_by_external: ['제품 스펙 확정'] },
  ]);
  const html = nodePanelHtml(g.nodes[0], { matched: false }, g);
  assert.match(html, /제품 스펙 확정/);
  assert.ok(!html.includes('<b>x</b>'), '작업명이 그대로 들어가면 안 된다');
  assert.match(html, /&lt;b&gt;/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test 테스트/graph.test.js`
Expected: FAIL — `nodePanelHtml` 이 함수가 아님(undefined)

- [ ] **Step 3: 함수 추가** — `B_직접/앱/dag-view.js`에서 아래를 찾아

```js
  var api = { renderDag: renderDag, renderDagWarnings: renderDagWarnings, deptColor: deptColor };
```

다음으로 교체 (함수 정의 + api 확장):

```js
  function statusClass(st) { return { '확정': 'ok', '추정': 'est', '확인필요': 'chk' }[st] || 'chk'; }

  // 노드 상세 패널 본문(순수 문자열). detail은 board-derive.buildDetailMap의 항목.
  function nodePanelHtml(node, detail, graph) {
    detail = detail || { matched: false };
    var byId = {}; (graph.nodes || []).forEach(function (n) { byId[n.id] = n; });
    var preds = (graph.edges || []).filter(function (e) { return e.to === node.id; })
      .map(function (e) { return byId[e.from] ? byId[e.from].task : e.from; });

    var h = '<div class="np-head"><span class="np-dept" style="background:' + deptColor(node.dept) + '"></span>'
          + '<div><div class="np-title">' + esc(node.task) + '</div>'
          + '<div class="np-sub">' + esc(node.dept || '[미상]') + '</div></div></div>';

    if (node.isBottleneck) {
      h += '<div class="np-bott">⚠ 병목 · 후행 ' + node.downstream + '개가 이걸 기다림</div>';
    }

    if (detail.matched) {
      h += '<dl class="np-facts">'
        + '<dt>담당</dt><dd>' + esc(detail.owner || '미정') + '</dd>'
        + '<dt>기한</dt><dd>' + esc(detail.due || '미정') + '</dd>'
        + '<dt>상태</dt><dd>' + (detail.status ? '<span class="tag ' + statusClass(detail.status) + '">' + esc(detail.status) + '</span>' : '미정') + '</dd>'
        + '</dl>';
      if (detail.basis) h += '<div class="np-basis">↳ ' + esc(detail.basis) + '</div>';
    } else {
      h += '<div class="np-note">담당·기한·상태는 위 <b>부서별 실행 항목</b> 표에서 확인하세요.</div>';
    }

    if (preds.length) {
      h += '<div class="np-block"><div class="np-label">선행</div>'
        + preds.map(function (t) { return '<div class="np-dep">← ' + esc(t) + '</div>'; }).join('') + '</div>';
    }
    if (node.externals && node.externals.length) {
      h += '<div class="np-block"><div class="np-label">외부 선행 · 회의록에 없음</div>'
        + node.externals.map(function (x) { return '<div class="np-dep np-ext">? ' + esc(x) + '</div>'; }).join('') + '</div>';
    }
    return h;
  }

  var api = { renderDag: renderDag, renderDagWarnings: renderDagWarnings, deptColor: deptColor, nodePanelHtml: nodePanelHtml };
```

- [ ] **Step 4: A로 복사**

Run: `cp "B_직접/앱/dag-view.js" "A_웹페이지/앱/dag-view.js"`

- [ ] **Step 5: 통과 확인**

Run: `node --test 테스트/graph.test.js`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add "테스트/graph.test.js" "A_웹페이지/앱/dag-view.js" "B_직접/앱/dag-view.js"
git commit -m "feat(뷰): nodePanelHtml — 노드 상세 패널 본문(담당/기한/상태/선행)(#2)"
```

---

## Task 6: index.html — 노드 클릭 → 사이드 패널 (#2)

DOM/CSS/이벤트라 단위 테스트 대신 **Playwright 시각 검증**. 아래 5개 편집(E1~E5)을 **A_웹페이지/앱/index.html 과 B_직접/앱/index.html 양쪽에 동일 텍스트로** 적용한다(두 파일의 앵커 문자열은 바이트 동일, 줄번호만 다름). 그리고 검증용 fixture를 만든다.

**Files:**
- Modify: `A_웹페이지/앱/index.html`, `B_직접/앱/index.html`
- Create: `테스트/fixtures/샘플01_풀보드.json`

- [ ] **Step 1: 검증용 fixture 생성** — `테스트/fixtures/샘플01_풀보드.json`

```json
{
  "meeting": { "title": "v2 신용평가 모델 회의", "date": "7/10(목) 15:00", "attendees": ["라일라PM","김상무","박리드","정팀장","이차장","최책임","한과장","윤매니저"] },
  "headline": "v2 모델 재학습(AUC 0.85)이 이사회·PoC·배포를 모두 막는 병목이다",
  "sequence": [
    { "id": "T1", "task": "대체데이터 개인정보 검토의견 작성", "dept": "법무실", "blocked_by": [], "blocked_by_external": [] },
    { "id": "T2", "task": "v2 모델 재학습 → AUC 0.85 달성", "dept": "CB본부", "blocked_by": [], "blocked_by_external": [] },
    { "id": "T3", "task": "가격 단가표 초안 작성", "dept": "경영본부", "blocked_by": [], "blocked_by_external": [] },
    { "id": "T4", "task": "가격정책 확정", "dept": "경영본부", "blocked_by": ["T3"], "blocked_by_external": [] },
    { "id": "T5", "task": "API 응답속도 최적화·배포", "dept": "ICT본부", "blocked_by": ["T2"], "blocked_by_external": [] },
    { "id": "T6", "task": "PoC 데모환경·샘플리포트 준비", "dept": "경영본부", "blocked_by": ["T2","T5"], "blocked_by_external": [] },
    { "id": "T7", "task": "이사회 보고자료 초안(7/18)", "dept": "경영본부", "blocked_by": ["T2","T4"], "blocked_by_external": [] },
    { "id": "T8", "task": "저축은행 3곳 PoC 시연(8/15)", "dept": "사업솔루션본부", "blocked_by": ["T6"], "blocked_by_external": [] },
    { "id": "T9", "task": "마케팅 브로셔·랜딩 제작", "dept": "마케팅본부", "blocked_by": [], "blocked_by_external": ["제품 스펙 확정"] },
    { "id": "T10", "task": "대체데이터 모델 반영 여부 결정", "dept": "법무실", "blocked_by": ["T1"], "blocked_by_external": [] }
  ],
  "by_department": [
    { "dept": "CB본부", "action_items": [
      { "what": "피처 추가 후 v2 모델 재학습 (AUC 0.85 목표, 현재 0.82)", "owner": "박리드", "due": "확인 필요", "status": "확인필요" },
      { "what": "AUC 0.85 달성 시 이사회 상정 검토", "owner": "김상무", "due": "", "status": "추정" } ],
      "documents": [], "decisions_needed": [ { "topic": "이사회 상정 여부", "decider": "김상무", "due": "확인 필요" } ] },
    { "dept": "ICT본부", "action_items": [
      { "what": "API 응답속도 최적화·배포 (캐싱, 모델 서빙)", "owner": "정팀장", "due": "확인 필요", "status": "확인필요" } ],
      "documents": [], "decisions_needed": [] },
    { "dept": "경영본부", "action_items": [
      { "what": "가격 단가표 초안 작성", "owner": "한과장", "due": "이달 말?", "status": "확인필요" },
      { "what": "이사회 보고자료 초안 작성", "owner": "라일라PM", "due": "7/18", "status": "확정" } ],
      "documents": [], "decisions_needed": [] },
    { "dept": "사업솔루션본부", "action_items": [
      { "what": "저축은행 3곳 PoC 시연", "owner": "이차장", "due": "8/15", "status": "확정" } ],
      "documents": [], "decisions_needed": [] },
    { "dept": "법무실", "action_items": [
      { "what": "대체데이터 개인정보 검토의견 작성", "owner": "최책임", "due": "다음주 화요일", "status": "확정" } ],
      "documents": [ { "doc": "대체데이터 개인정보 검토의견", "owner": "최책임", "due": "다음주 화요일" } ], "decisions_needed": [] },
    { "dept": "마케팅본부", "action_items": [],
      "documents": [ { "doc": "브로셔·랜딩페이지" } ], "decisions_needed": [] }
  ],
  "gaps": ["v2 모델 재학습 완료 기한 미기재","가격 단가표·가격정책 기한 미확정 (이달 말?)","API 응답속도 최적화 기한 미기재","제품 스펙 확정 시점 미정 (마케팅 착수 선행)","가격 미확정인데 이사회 보고자료에 가격 포함 필요 → 순서 충돌","데사팀 채용 1명은 별건으로 보류"],
  "assumptions": ["8/15 PoC 시연은 확정 일정으로 가정","이사회는 다음주 금요일로 가정"]
}
```

- [ ] **Step 2 (E1): board-derive.js 스크립트 로드** — 양쪽 파일에서

```html
<script src="dag-view.js"></script>
```

를 찾아 바로 다음 줄에 추가:

```html
<script src="board-derive.js"></script>
```

- [ ] **Step 3 (E2): 패널·더보기 CSS 추가** — 양쪽 파일의 `<style>` 안, `/* gaps / assumptions */` 주석을 찾아 그 **앞에** 삽입

```css
  /* 노드 상세 사이드 패널 (#2) */
  .dag-n[data-id]{cursor:pointer}
  .dag-n.sel rect:first-child{stroke:var(--primary);stroke-width:3}
  .np-overlay{position:fixed;inset:0;background:rgba(4,10,20,.28);opacity:0;pointer-events:none;transition:.2s;z-index:55}
  .np-overlay.on{opacity:1;pointer-events:auto}
  .node-panel{position:fixed;top:0;right:0;height:100vh;width:min(360px,88vw);z-index:60;
    background:var(--surface);border-left:1px solid var(--line);box-shadow:var(--shadow);
    transform:translateX(100%);transition:transform .22s;overflow:auto;padding:18px 18px 28px}
  .node-panel.on{transform:translateX(0)}
  .np-close{float:right;width:30px;height:30px;border-radius:8px;border:1px solid var(--line);
    background:var(--surface2);color:var(--ink2);cursor:pointer;font-size:14px;line-height:1}
  .np-close:hover{border-color:var(--primary)}
  .np-head{display:flex;gap:10px;align-items:flex-start;margin:2px 34px 12px 0}
  .np-dept{width:8px;height:38px;border-radius:4px;flex:none;margin-top:2px}
  .np-title{font-size:15px;font-weight:800;line-height:1.3}
  .np-sub{font-size:12px;color:var(--muted);margin-top:2px}
  .np-bott{font-size:12px;font-weight:700;color:var(--chk);background:var(--chk-bg);border-radius:8px;padding:7px 10px;margin-bottom:12px}
  .np-facts{display:grid;grid-template-columns:auto 1fr;gap:6px 12px;margin:0 0 10px}
  .np-facts dt{font-size:11px;color:var(--faint);font-weight:600}
  .np-facts dd{margin:0;font-size:13px;color:var(--ink);font-family:var(--mono)}
  .np-basis{font-size:12px;color:var(--muted);margin-bottom:10px}
  .np-note{font-size:12.5px;color:var(--muted);background:var(--surface2);border:1px dashed var(--line);border-radius:8px;padding:9px 11px;margin-bottom:10px}
  .np-block{margin-top:10px}
  .np-label{font-family:var(--mono);font-size:10px;font-weight:600;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);margin-bottom:4px}
  .np-dep{font-size:12px;color:var(--ink2);padding:3px 0}
  .np-dep.np-ext{color:var(--muted);font-family:var(--mono)}
  @media(max-width:600px){
    .node-panel{top:auto;bottom:0;right:0;height:auto;max-height:82vh;width:100%;border-left:0;border-top:1px solid var(--line);transform:translateY(100%)}
    .node-panel.on{transform:translateY(0)}
  }
  /* 더보기 토글 (#3) */
  details.more{margin-top:2px}
  details.more>summary{cursor:pointer;list-style:none;color:var(--muted);font-family:var(--mono);font-size:11px;padding:5px 0}
  details.more>summary::-webkit-details-marker{display:none}
  details.more>summary:hover{color:var(--primary-ink)}
  details.more[open]>summary{margin-bottom:6px}
```

- [ ] **Step 4 (E3): 패널 DOM 추가** — 양쪽 파일에서

```html
<div class="toast" id="toast"></div>
```

를 찾아 바로 다음에 추가:

```html
<div class="np-overlay" id="npOverlay"></div>
<aside class="node-panel" id="nodePanel" role="dialog" aria-label="작업 상세" aria-modal="true">
  <button class="np-close" id="npClose" aria-label="닫기">✕</button>
  <div id="npBody"></div>
</aside>
```

- [ ] **Step 5 (E4): 패널 열기/닫기 헬퍼 + 전역 핸들러** — 양쪽 파일에서 `function render(raw){` 를 찾아 그 **앞에** 삽입

```js
/* 노드 상세 패널 (#2) */
function openPanel(html){$('#npBody').innerHTML=html;$('#nodePanel').classList.add('on');$('#npOverlay').classList.add('on');}
function closePanel(){$('#nodePanel').classList.remove('on');$('#npOverlay').classList.remove('on');
  document.querySelectorAll('.dag-n.sel').forEach(n=>n.classList.remove('sel'));}
$('#npClose').onclick=closePanel;$('#npOverlay').onclick=closePanel;
document.addEventListener('keydown',e=>{if(e.key==='Escape')closePanel();});
/* 우선순위 칩(#3): 랭킹 → top5 + 더보기 */
function rankedChips(items,cls,graph){
  const ranked=BoardDerive.rankItems(graph,items||[]).map(r=>r.text);
  if(!ranked.length)return '<span class="chip">없음</span>';
  const chip=t=>`<span class="chip ${cls}">${esc(t)}</span>`;
  const top=ranked.slice(0,5),rest=ranked.slice(5);
  let out=top.map(chip).join('');
  if(rest.length)out+=`<details class="more"><summary>더보기 (+${rest.length})</summary>${rest.map(chip).join('')}</details>`;
  return out;
}
```

- [ ] **Step 6 (E5): 노드 클릭 배선** — 양쪽 파일에서

```js
  document.querySelectorAll('.msg .cp').forEach(b=>b.onclick=()=>copy($('#'+b.dataset.c).textContent));
```

를 찾아 바로 다음에 삽입 (이 지점에서 `g`, `d`는 스코프에 있음):

```js
  closePanel();
  const detail=BoardDerive.buildDetailMap(g,d.by_department);
  document.querySelectorAll('#result .dag-n[data-id]').forEach(el=>{
    const open=()=>{const n=g.nodes.find(x=>x.id===el.dataset.id);if(!n)return;
      document.querySelectorAll('.dag-n.sel').forEach(s=>s.classList.remove('sel'));el.classList.add('sel');
      openPanel(DagView.nodePanelHtml(n,detail[n.id],g));el.focus();};
    el.addEventListener('click',open);
    el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}});
  });
```

- [ ] **Step 7: Playwright 시각 검증** (B 앱, file://)

1. `browser_navigate` → `file:///C:/Users/toyal/Desktop/새 폴더/B_직접/앱/index.html`
2. 기본은 복붙 모드. `테스트/fixtures/샘플01_풀보드.json` 내용을 `browser_evaluate`로 주입 렌더:
   `() => render(JSON.parse(document.querySelector('#seed')?.textContent || '<위 JSON 문자열>'))` — 간단히는 `browser_evaluate`에 fixture 객체를 인라인으로 넘겨 `render(obj)` 호출.
3. `browser_snapshot`으로 DAG의 병목 노드(빨강, "v2 모델 재학습") 확인 → 그 노드 `browser_click`.
4. 패널이 열리고 **담당=박리드 · 기한=확인 필요 · 상태=확인필요 · ⚠ 병목 · 후행 4개**가 보이는지 `browser_snapshot` + `browser_take_screenshot`.
5. `#themeBtn` 클릭(다크) → 패널 스크린샷. `Esc`로 닫힘 확인.

Expected: 병목 노드 빨간 테두리 유지 + 선택 시 primary 링, 패널에 담당/기한/상태/후행 4개 표시, 라이트·다크 모두 정상.

- [ ] **Step 8: 커밋**

```bash
git add "테스트/fixtures/샘플01_풀보드.json" "A_웹페이지/앱/index.html" "B_직접/앱/index.html"
git commit -m "feat(보드): 노드 클릭 → 사이드 패널(담당/기한/상태/병목/선행)(#2)"
```

---

## Task 7: index.html — 확인필요·가정 랭킹 + top5 + 더보기 (#3)

**Files:**
- Modify: `A_웹페이지/앱/index.html`, `B_직접/앱/index.html` (섹션 03 렌더 2줄 교체)

> `rankedChips` 헬퍼는 Task 6 E4에서 이미 추가됨. 여기선 gaps/assumptions 렌더 호출만 교체.

- [ ] **Step 1: gaps 렌더 교체** — 양쪽 파일에서

```js
    h+=`<div class="panelbox"><div class="block__l">❓ 확인 필요 (gaps)</div><div class="chips">${(d.gaps||[]).filter(Boolean).map(g=>`<span class="chip gap">${esc(g)}</span>`).join('')||'<span class="chip">없음</span>'}</div></div>`;
```

를 다음으로 교체:

```js
    h+=`<div class="panelbox"><div class="block__l">❓ 확인 필요 (gaps)</div><div class="chips">${rankedChips(d.gaps,'gap',g)}</div></div>`;
```

- [ ] **Step 2: assumptions 렌더 교체** — 양쪽 파일에서

```js
    h+=`<div class="panelbox"><div class="block__l">🧩 가정 (assumptions)</div><div class="chips">${(d.assumptions||[]).filter(Boolean).map(a=>`<span class="chip asm">${esc(a)}</span>`).join('')||'<span class="chip">없음</span>'}</div></div>`;
```

를 다음으로 교체:

```js
    h+=`<div class="panelbox"><div class="block__l">🧩 가정 (assumptions)</div><div class="chips">${rankedChips(d.assumptions,'asm',g)}</div></div>`;
```

- [ ] **Step 3: Playwright 시각 검증** (Task 6 Step 7의 렌더 상태에서)

1. 섹션 03 "확인 필요"에 칩이 **5개 이하 표시** + **"더보기 (+1)"** 토글(gaps 6개 중 5개 노출).
2. 첫 칩이 **"v2 모델 재학습 완료 기한 미기재"**(병목 gap)인지 `browser_snapshot`.
3. "더보기" `browser_click` → "데사팀 채용 1명은 별건으로 보류"가 펼쳐지는지 확인.
4. `browser_take_screenshot`.

Expected: 병목 gap이 최상단, 별건 gap이 더보기 안. assumptions(2개)는 더보기 없음.

- [ ] **Step 4: 커밋**

```bash
git add "A_웹페이지/앱/index.html" "B_직접/앱/index.html"
git commit -m "feat(보드): 확인필요·가정 그래프 랭킹 + top5 + 더보기(#3)"
```

---

## Task 8: index.html — 부서 카드 높이/여백 정리 (#4)

**Files:**
- Modify: `A_웹페이지/앱/index.html`, `B_직접/앱/index.html` (`.depts` 규칙 1곳)

- [ ] **Step 1: `.depts`에 `align-items:start` 추가** — 양쪽 파일에서

```css
  .depts{display:grid;grid-template-columns:repeat(auto-fill,minmax(360px,1fr));gap:14px}
```

를 다음으로 교체:

```css
  .depts{display:grid;grid-template-columns:repeat(auto-fill,minmax(360px,1fr));gap:14px;align-items:start}
```

- [ ] **Step 2: Playwright 시각 검증** (풀보드 렌더 상태)

1. 섹션 01 부서 카드에서 **마케팅본부**(작성 문서 1개만) 카드가 같은 행의 **경영본부**(할일+의사결정 등 다중) 카드 높이에 **억지로 늘어나지 않고 자연 높이**인지 `browser_take_screenshot`으로 확인.
2. 마케팅본부 카드에 **빈 "할 일"/"의사결정" 섹션이 없는지**(조건부 렌더, 이미 동작) 확인 = #4 회귀 확인.

Expected: 1섹션·다섹션 카드가 각자 자연 높이, 빈 섹션 없음, 하단 여백 균일.

- [ ] **Step 3: 커밋**

```bash
git add "A_웹페이지/앱/index.html" "B_직접/앱/index.html"
git commit -m "style(보드): 부서 카드 자연 높이(align-items:start) — 스캔성(#4)"
```

---

## Task 9: index.html — 정보 위계 (히어로 + 아코디언) (#5)

`지시사항.md` item 9 지시. 서머리+크리티컬 패스 DAG를 최상단 히어로로, 나머지 3그룹(부서별 실행항목·확인필요/가정·부서별 메시지)을 개수 배지 붙인 아코디언으로 기본 접힘. **Task 6·7·8 뒤에 실행**(최종 섹션 내용 확정 후 위계로 감싼다). DOM/CSS라 Playwright 시각 검증. 아래 편집을 **A·B 양쪽 동일 텍스트**로 적용.

**Files:**
- Modify: `A_웹페이지/앱/index.html`, `B_직접/앱/index.html`

- [ ] **Step 1 (CSS): 아코디언 스타일 추가** — 양쪽 파일에서 `/* 더보기 토글 (#3) */` 주석을 찾아 그 **앞에** 삽입

```css
  /* 정보 위계 아코디언 (#5) */
  details.acc{background:var(--surface);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow-sm);margin:14px 2px 0;overflow:hidden}
  details.acc>summary{cursor:pointer;list-style:none;display:flex;align-items:center;gap:10px;padding:14px 16px;font-size:14.5px;font-weight:800;letter-spacing:-.01em}
  details.acc>summary::-webkit-details-marker{display:none}
  details.acc>summary::before{content:"▸";color:var(--muted);font-size:11px;transition:transform .15s}
  details.acc[open]>summary::before{transform:rotate(90deg)}
  details.acc>summary:hover{color:var(--primary-ink)}
  details.acc .cnt{margin-left:auto;font-family:var(--mono);font-size:11px;font-weight:700;color:var(--muted);background:var(--surface2);border:1px solid var(--line);border-radius:999px;padding:2px 9px}
  details.acc>.acc-body{padding:0 16px 16px}
```

- [ ] **Step 2 (JS 헬퍼): `acc()` 추가** — 양쪽 파일에서 (Task 6에서 추가한) `function rankedChips(` 정의를 찾아 그 **앞에** 삽입

```js
function acc(title,count,bodyHtml){
  return `<details class="acc"><summary><span class="acc-t">${esc(title)}</span><span class="cnt">${count}</span></summary><div class="acc-body">${bodyHtml}</div></details>`;
}
```

- [ ] **Step 3 (재배치): render()의 섹션 조립 블록 교체** — 양쪽 파일의 `render()` 안에서, `h+=secH('01','부서별 실행 항목');`(부서 섹션 시작)부터 `share_messages` 블록 끝(`});` 직후, `h+=\`<details class="raw">\`` **직전**까지의 **전 구간**을 아래로 교체. (Task 7의 `rankedChips` 호출을 그대로 품는다.)

교체 후 코드:

```js
  const g = Graph.buildGraph(d.sequence||[]);

  // ── 히어로: 크리티컬 패스 DAG (최상단 강조) ──
  h+=secH('01','권장 진행 순서 · 크리티컬 패스');
  h+=DagView.renderDagWarnings(g);
  if(g.nodes.length===1){
    h+=`<div class="dag-single">${esc(g.nodes[0].task)} · ${esc(g.nodes[0].dept)}</div>`;
  } else if(g.nodes.length){
    h+=`<div class="dag-wrap">${DagView.renderDag(g)}</div>`;
  }

  // ── 아코디언 3그룹 (기본 접힘) ──
  const deptsHtml='<div class="depts">'+ (d.by_department||[]).map(dep=>
    `<section class="dept"><div class="dept__h"><span class="dot"></span><span class="nm">${esc(dep.dept)}</span></div><div class="dept__b">`+
    (tblAI(dep.action_items)+tblDoc(dep.documents)+tblDec(dep.decisions_needed) || '<div class="block"><div class="basis">추출된 항목 없음</div></div>')+
    `</div></section>`).join('') + '</div>';
  h+=acc('부서별 실행 항목', (d.by_department||[]).length, deptsHtml);

  if((d.gaps&&d.gaps.length)||(d.assumptions&&d.assumptions.length)){
    const gc=(d.gaps||[]).filter(Boolean).length, asmc=(d.assumptions||[]).filter(Boolean).length;
    const body='<div class="cols2">'
      +`<div class="panelbox"><div class="block__l">❓ 확인 필요 (gaps)</div><div class="chips">${rankedChips(d.gaps,'gap',g)}</div></div>`
      +`<div class="panelbox"><div class="block__l">🧩 가정 (assumptions)</div><div class="chips">${rankedChips(d.assumptions,'asm',g)}</div></div>`
      +'</div>';
    h+=acc('확인 필요 · 가정', gc+asmc, body);
  }

  if(d.share_messages&&d.share_messages.length){
    let body='';
    d.share_messages.forEach((m,i)=>{
      if(m.chat)body+=`<div class="msg"><div class="mh">💬 ${esc(m.dept)}<span class="kind">메신저</span><button class="btn mini cp" data-c="c${i}">복사</button></div><pre id="c${i}">${esc(m.chat)}</pre></div>`;
      if(m.email_body)body+=`<div class="msg"><div class="mh">✉️ ${esc(m.dept)}<span class="kind">이메일</span><button class="btn mini cp" data-c="e${i}">복사</button></div><pre id="e${i}">${esc((m.email_subject?('제목: '+m.email_subject+'\n\n'):'')+m.email_body)}</pre></div>`;
    });
    h+=acc('부서별 공유 메시지', d.share_messages.length, body);
  }
```

> 주의: 원래 `share_messages` 는 `d.share_messages.forEach` 가 `h+=` 로 직접 붙였다. 새 코드는 `body` 문자열에 모아 `acc()` 로 감싼다 — `.msg .cp` 복사 버튼 배선(render 끝)은 그대로 동작(아코디언 안에서도 DOM에 존재). `const g` 를 이 블록 맨 위로 올렸으므로, Task 6에서 추가한 `const detail=BoardDerive.buildDetailMap(g,d.by_department)` (render 끝, `g` 사용)와 `rankedChips(...,g)` 는 여전히 스코프 안이다.

- [ ] **Step 4: Playwright 시각 검증** (풀보드 렌더)

1. `B_직접/앱/index.html` file:// 로드 → 풀보드 fixture 주입 렌더.
2. 초기 상태: **서머리 + 크리티컬 패스 DAG만 보이고**, "부서별 실행 항목", "확인 필요 · 가정", "부서별 공유 메시지" 3개는 **접힌 아코디언**(각 summary + 개수 배지)인지 `browser_take_screenshot`.
3. "부서별 실행 항목" summary `browser_click` → 펼쳐지고 부서 카드가 나오는지 확인(#4 카드 정렬도 여기서 재확인).
4. DAG 노드 클릭 → 패널 정상(#2 무회귀), 확인필요 아코디언 펼쳐 top5+더보기(#3 무회귀) 확인.
5. 다크 테마 스크린샷.

Expected: 히어로(서머리+DAG)만 노출, 3그룹 접힘·배지 정확, 펼치면 각 내용 정상, #2·#3·#4 무회귀. 라이트·다크.

- [ ] **Step 5: 커밋**

```bash
git add "A_웹페이지/앱/index.html" "B_직접/앱/index.html"
git commit -m "feat(보드): 정보 위계 — 히어로(서머리+DAG) + 아코디언 3그룹(#5)"
```

---

## Task 10: 최종 통합 검증

**Files:** 없음(검증만)

- [ ] **Step 1: 전체 단위 테스트 green**

Run: `node --test 테스트/graph.test.js`
Expected: 종료코드 0. baseline 26 + 신규(노출1·조인1·랭킹3·data-id1·패널3 = 9) 전부 통과.

- [ ] **Step 2: A·B 3개 순수 모듈 바이트 동일 재확인**

Run: `git diff --no-index "A_웹페이지/앱/graph.js" "B_직접/앱/graph.js"; git diff --no-index "A_웹페이지/앱/dag-view.js" "B_직접/앱/dag-view.js"; git diff --no-index "A_웹페이지/앱/board-derive.js" "B_직접/앱/board-derive.js"`
Expected: 출력 없음(차이 0). (테스트로도 강제되지만 이중 확인)

- [ ] **Step 3: A 앱도 동일 동작 확인** — Playwright로 `A_웹페이지/앱/index.html`에 대해 Task 6 Step 7 반복(노드 클릭 패널). Expected: B와 동일.

- [ ] **Step 4: 최종 스크린샷 보관** — 라이트·다크 각 (a) 히어로(서머리+DAG)+접힌 아코디언 3그룹 (b) 노드 패널 (c) 확인필요 펼침 top5+더보기 (d) 부서별 펼침 1 vs 3섹션 카드. `문서/스크린샷/`에 저장(선택).

- [ ] **Step 5: 완료 커밋(있으면)** — 스크린샷만.

```bash
git add "문서/스크린샷/"
git commit -m "docs: 실행보드 v2 개선 검증 스크린샷(라이트/다크)"
```

---

## Self-Review (작성자 점검 완료)

- **Spec 커버리지**: #2(조인 T2·T5·패널=Task2/5/6) · #3(랭킹 top5 더보기=Task3/7) · #4(조건부 렌더 회귀=Task8 Step2, 카드 여백=Task8) · 공통 인프라(graph 노출=Task1, board-derive=Task2/3, dag-view=Task4/5, A·B 동일성=각 태스크+Task9) — 모두 태스크로 매핑됨.
- **플레이스홀더**: 없음. 모든 코드/명령/기대값이 구체값(실측 containment·정렬순서 포함).
- **타입 일관성**: `buildDetailMap`/`rankItems`/`nodePanelHtml` 시그니처가 Task 간 동일. `rankedChips(items,cls,graph)`는 render의 `g`(그래프)와 `d.by_department`를 사용 — 둘 다 render 스코프에 존재(확인함). 섹션 03의 기존 `.map(g=>…)` 셰도잉은 해당 줄 교체로 제거됨.
- **임계값**: 조인 0.5 / 랭킹 0.35 분리 — 실측(병목 gap 0.46)으로 검증됨.
